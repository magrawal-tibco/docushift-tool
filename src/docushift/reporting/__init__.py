"""Findings, and later the commands that read them.

`findings.py` -- the table, the code registry and `record()` -- landed in Phase 5a
(`planning.md` §7.1, §7.5, §7.7). The `report` / `validate` / `csh` commands that
query it are Phase 7.
"""
