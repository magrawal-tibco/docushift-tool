# Project Context & Living Ledger: DocuShift Tool

> **Last Updated:** 2026-09-10  
> **Status:** Phases 1-3 complete and verified — `catalog fetch` runs against the live docsite (256 tests pass, lint clean); Phase 4's selection model and path contract also landed, so what remains there is the acquisition I/O. The Phase 5 **CSH design is settled** (`architecture.md` §5.4) and **all three HTML converter engines are specified** — Flare (§5.1), SDL DITA (§5.2) and WebWorks (§5.3) — all awaiting implementation; Phase 4's **extraction inventory columns are specified** (§3.9) and awaiting implementation. **Stage 7 stops at the filesystem** — git operations are out of scope (§6.0, 2026-09-09). **Phase 3.5 is complete** — 61 EBX/Spotfire products are permanently out of scope, enforced by `config/scope.yaml` and the `in_scope` gate, in place before the first full fetch (§3.10). **Phase 3.6 is complete** — the catalog is keyed on the docsite `slug`, not `product_code`, which 21 products share (§3.1). **The catalog now exists**: `catalog fetch --all --include-archived` ran clean on 2026-09-10 and wrote **634 products / 4,462 versions**, 60 products out of scope. **Phase 3.7 is complete** — support's end-of-support report is a committed input, re-applied by `catalog eos`, and `Retired` is the second of four selection gates: **128 in-scope eligible versions retired across 11 emptied products**, report coverage **251 of 634** products, 0 stale aliases (§3.11). **404 tests pass, lint clean**  
> **Primary Runtime:** Python 3.11+ (Active: Python 3.13)  
> **Business Scope:** TIBCO & IBI Documentation Migration (~250 Products) to AEM on GitHub — DocuShift produces the repo-shaped trees; publishing them is a separate step

---

## 1. Project Overview & Business Mission
**DocuShift** is an enterprise-grade documentation migration, transformation, and sync pipeline built to transition ~250 products across two major Business Units (**TIBCO** and **IBI**) from legacy docsite publishing (`docs.tibco.com`) into modern **Adobe Experience Manager (AEM)** documentation repositories hosted on **GitHub**.

### The 7 Core Pipeline Pillars
1. **Catalog & Discovery (On-Demand & Additive)**:
   - Queries `docs.tibco.com/a_z_products` and underlying APIs (`/api/a_to_z`, `/api/products/{slug}`, `/api/products/archive/{slug}`, `/api/product_list_by_suites`).
   - Discovers **Active Versions** (with "Download All Docs" ZIP endpoints) and **Archived Versions** ("Other Versions" archive index).
   - **Product Scope**: 61 EBX/Spotfire products are permanently excluded from conversion via `config/scope.yaml`, matched by exact docsite slug and recorded as `in_scope=false` in `products.csv`. They stay fully catalogued — excluded is not absent (`architecture.md` §3.10).
   - **End-of-Support**: support's retirement report is committed under `config/eos/` and re-applied by `catalog eos`, which resolves `release_status` / `retirement_date` / `release_status_source` onto each version row. A `retired` version is skipped by every working command; **only `retired` gates** — a dated retirement *announcement* still converts, and a version with no row is `unknown`, never retired (`architecture.md` §3.11).
   - **Conversion Policy**: Active versions are flagged `convert_eligible: true` by default; archived versions are captured for full inventory (`is_archived: true`, `convert_eligible: false` by default, convertable only if explicitly marked).
   - **Smart Merge**: Additive snapshot-based 3-way merge into `config/products.csv` and `config/versions.csv` that preserves manual user edits without requiring a manual override flag.
2. **Taxonomy & Family Mapping**: Categorize products into structured Product Families (e.g., Messaging, Integration, Analytics, WebFOCUS, Omni, Data Management). Per-product assignment lives in `config/products.csv` (bulk-editable); `config/taxonomy.yaml` holds family definitions and keyword inference rules only. Classification is majority-manual — the docsite's own category data covers only a minority of products.
3. **Package Acquisition & Cache**: Resumable downloader with checksumming and rate-limiting, writing doc ZIPs into the per-family workspace `families/{locale}-{bu}-{family}/downloads/`. Selection is `convert_eligible` (policy) narrowed by `convert_batch` (which run), so a POC converts three versions without touching the other ~1,500 rows. Where discovery yields no usable `zip_url`, a hand-obtained ZIP is ingested with `--from-file` and pinned `zip_source=manual` — it lands at the same canonical path and converts through the same code path as a downloaded one.
4. **Extraction & Asset Cataloger**: Unzip into `families/{...}/extracted/{product}/{version}/`, discover HTML docs, Context-Sensitive Help (CSH) maps, and binary/document assets (`PDF`, `DOC`, `XLS`, `TXT`, `PNG`, `SVG`, `ZIP`). Runs over the same selection as the downloader, so archived versions are neither fetched nor unpacked.
5. **Multi-Engine Conversion to GFM**: Convert HTML (primarily MadCap Flare + DITA/WebWorks/DocBook) into clean GitHub-Flavored Markdown tailored for AEM. The engine is a **per-version** property detected from package contents — the same product commonly spans generators across its version history. Conversion also emits the version's **context-sensitive help map** (`csh.yml`) and stamps the matching identifiers into topic frontmatter, so a product's Help button still resolves after migration.
6. **AEM Navigation & Architecture Synthesis**: Generate AEM-required navigation trees (`toc.yml`, `nav.yml`, `meta.yml`), landing pages, and frontmatter.
7. **Publishing Layout Assembly**: Organize converted output into repo-shaped directory trees on disk, named and laid out exactly as the target GitHub repositories are. **Git operations are out of scope** — branching, committing, review and pushing are picked up separately (`architecture.md` §6.0).

---

## 2. Directory Structure Standards

```
docushift-tool/
├── .gitignore              # Ignored caches, downloads, output, state DBs
├── .gitmessage             # Commit message template (User Requests / Changes / Technical Details)
├── CONTEXT.md              # Living state & decision ledger (This file)
├── README.md               # Repository landing page & quickstart
├── pyproject.toml          # Python package manifest, dependencies, test config
├── docs/                   # Living Project Documentation
│   ├── architecture.md     # Full 7-stage pipeline architecture, additive catalog, data schemas
│   ├── design.md           # Logic & algorithms in standard English, marked Built vs Specified
│   ├── user-guide.md       # CLI reference, batch workflows, manual catalog editing
│   └── planning.md         # Master roadmap, milestones, validation criteria
├── config/                 # Configurations, Taxonomies & Catalog
│   ├── products.csv        # Master Product registry (one row per product; bulk-editable in Excel)
│   ├── versions.csv        # Master Version registry (one row per version; convert_eligible toggles)
│   ├── taxonomy.yaml       # Family definitions per BU + keyword inference rules (no per-product rows)
│   ├── docsite.yaml        # docs.tibco.com discovery endpoints & crawling rules
│   ├── scope.yaml          # 61 products never converted, matched by exact docsite slug (§3.10)
│   ├── eos.yaml            # Points at the support report + 14 reviewed name aliases (§3.11)
│   ├── eos/                # Support's end-of-support report, committed as received
│   ├── publishing.yaml     # Stage 7: publish_base_url + doc-class-to-repo map (Phase 6)
│   └── aem_templates/      # YAML/Markdown templates for AEM toc.yml, nav.yml, and landing pages
├── src/                    # Source code
│   └── docushift/
│       ├── __init__.py
│       ├── cli.py          # Click CLI entrypoint (catalog, download, convert, sync, report)
│       ├── config.py       # Configuration & taxonomy manager
│       ├── catalog.py      # Additive Catalog Manager (CSV read/write + snapshot 3-way merge)
│       ├── state.py        # State engine (SQLite: fetch snapshots, etags, sizes, per-stage status)
│       ├── discovery/      # Stage 1: docs.tibco.com API crawler (Active & Archived versions)
│       ├── downloader/     # Stage 3: Resumable package downloader & cache manager
│       ├── extractor/      # Stage 4: ZIP extractor & asset/CSH analyzer
│       ├── engines/        # Stage 5: HTML Profile Extractors
│       │   ├── base.py     # BaseEngine interface
│       │   ├── detector.py # Profile auto-detector
│       │   ├── flare.py    # MadCap Flare handler (output roots, TOC .js, autonum, chrome)
│       │   ├── dita.py     # SDL DITA CMS handler
│       │   ├── webworks.py # WebWorks handler
│       │   └── docbook.py  # DocBook handler
│       ├── transforms/     # GFM Transformations
│       │   ├── callouts.py # Note/Warning/Tip to GFM alerts
│       │   ├── tables.py   # Table normalizer
│       │   ├── code.py     # Code syntax highlighter
│       │   ├── links.py    # Cross-document & anchor link resolver
│       │   ├── csh.py      # CSH schema, resolver & csh.yml writer (engine-neutral; engines supply readers)
│       │   └── assets.py   # Asset re-linker & copier
│       ├── aem/            # Stage 6: AEM Navigation & Architecture generator (TOC/YAML)
│       ├── sync/           # Stage 7: publishing-layout organizer (filesystem only, no git)
│       ├── reporting/      # Logging, metrics dashboard, audit generation
│       └── utils/          # Shared helpers
│           ├── csvio.py    # CSV round-trip hygiene (BOM, bools, dates, natural sort)
│           └── slug.py     # slugify() + family_folder() -> `en-us-tibco-messaging`
├── tests/                  # Automated Test Suite
│   ├── conftest.py         # Test fixtures
│   ├── unit/               # Unit tests per stage (catalog, discovery, parser, transforms, state)
│   ├── integration/        # End-to-end multi-product conversion tests
│   └── fixtures/           # Sample Flare zips, CSH maps, HTML docs, assets
├── cache/                  # (Git-ignored) state.db and scratch
├── families/               # (Git-ignored) Per-family working set — the download/extract target
│   └── en-us-tibco-messaging/      # {locale}-{bu}-{family}; doubles as the publishing repo name
│       ├── downloads/              # ems-10.4.0.zip  ({product_code}-{version}.zip)
│       ├── extracted/ems/10.4.0/   # version keeps its dots; dashes are a Stage 6 concern
│       └── archive/                # only via `docushift archive download`
└── output/                 # (Git-ignored) Final converted GFM output organized by BU/Family
    └── …/<product>/<version>/
        ├── csh.yml         # identifier -> topic map for this version (omitted when the package has none)
        ├── toc.yml
        └── <doc-set>/…     # converted topics, CSH identifiers stamped into frontmatter
```

Per-family folders are created on demand by the downloader, not at startup: pre-creating one per declared family would make an empty workspace look like a started migration.

---

## 3. Current State & Decision Ledger

| Date | Category | Decision / Update | Rationale / Note |
| :--- | :--- | :--- | :--- |
| 2026-09-02 | Discovery | Verified `docs.tibco.com` APIs (`/api/a_to_z`, `/api/products/{slug}`, `/api/products/archive/{slug}`) | Discovered exact JSON structures for Active versions ("Download All Docs" ZIP) and Archived versions ("Other Versions" ZIPs). |
| 2026-09-02 | Archive Policy | Default `convert_eligible: false` for archived versions | Captures complete product history while preventing unnecessary conversion of deprecated versions unless explicitly flagged. |
| 2026-09-02 | Catalog | **Optional & Additive Catalog** (`catalog.py` & `config/catalog.json`) ~~JSON storage~~ *(storage format superseded 2026-09-03; the on-demand/additive principle stands)* | Product version fetching is purely on-demand; fetches do additive smart-merges preserving manual user edits and overrides. |
| 2026-09-02 | Scope | Expanded scope to 7-Stage End-to-End Pipeline for ~250 products (TIBCO & IBI) | Covers complete lifecycle from `docs.tibco.com` discovery to GitHub/AEM sync. |
| 2026-09-02 | State Tracking | Built-in State Tracking Engine (`state.py`) | Essential for batching ~250 products, delta fetching, resume-on-failure, and progress metrics. |
| 2026-09-03 | Catalog Format | **CSV replaces JSON** as catalog source of truth: `config/products.csv` + `config/versions.csv`, normalized on `product_code`. `config/catalog.json` retired. | ~250 products x 5-15 versions = 1,500-4,000 rows. Dominant human edits are column operations (bulk-toggle `convert_eligible`, reassign `family`) — trivial in Excel, hostile in nested JSON. CSV also yields 1-line git diffs per new version vs. 10-line nested inserts. |
| 2026-09-03 | Catalog Schema | Volatile machine state (`zip_etag`, `zip_size`, checksums, per-stage status, `metadata` dict) moved out of the catalog into `state.db`. | Keeps the CSVs stable enough to leave open in Excel — `catalog fetch` should only touch them when discovery finds a genuinely new product or version. Prevents save conflicts and diff churn. |
| 2026-09-03 | Merge Strategy | Replaced flag-based protection with **snapshot-based 3-way merge**: `state.db` stores the last-fetched values; any CSV field differing from that snapshot is inferred to be a human edit and is protected automatically. `custom_override` demoted to an explicit whole-row pin. | Requiring the user to remember `custom_override=true` on each edited row of a 4,000-row sheet guarantees silent data loss. This also makes the merge genuinely 3-way (base + theirs + mine), matching what `architecture.md` already claimed. |
| 2026-09-03 | Taxonomy | `taxonomy.yaml` demoted to **family definitions + keyword inference rules only**; per-product `bu`/`family` assignment moves to `config/products.csv`. Hardcoded ibi/webfocus/omni/iway heuristics in `config.py` become YAML data. | Scaling 250 hand-classified products in 4-level nested YAML recreates the exact editing pain CSV was chosen to avoid. |
| 2026-09-03 | Engine Model | **`engine` moves from product-level to version-level** (`products.csv` → `versions.csv`; `Product.engine` → `ProductVersion.engine`). | Confirmed by user: the source toolchain varies across a product's version history — an older release may be DITA or FrameMaker/WebWorks while the latest is Flare. A product-level engine would apply the wrong converter to every older version. |
| 2026-09-03 | Engine Model | **Engine is detected, not declared.** Default changes from `flare` to `auto`; `engines/detector.py` resolves it from extracted content and writes back to `versions.csv`. New `engine_source` provenance (`manual` > `detected` > `auto`). Versions left at `auto` are skipped with a warning, never guessed. | Hand-assigning across 1,500-4,000 versions is infeasible. The old `SourceEngine.FLARE` default was silently destructive — a WebWorks set fed to the Flare engine yields plausible-looking but wrong Markdown with no error. Detection signals verified against the cached `dsp_gridserver` 7.1.1 sample: all six Flare marker paths present, `MadCap` reference in 197/197 `admin-guide` files. |
| 2026-09-03 | Pipeline | Catalog becomes a **mid-pipeline write target**: `engine` cannot be known at Stage 1 (Discovery) and is written back after Stage 4 (Extraction). | Engine resolution requires the package contents, so discovery can only leave it `auto`. Stage 5 reads the value to select a converter. |
| 2026-09-03 | Taxonomy | `taxonomy.yaml` keyword rules assign `bu`/`family` **only** — engine assertions removed. | Per-version engine variance makes any product-level engine rule wrong by construction. The existing `rendezvous`/`streambase`/`iprocess` → `docbook` mappings in `taxonomy.yaml` are product-level assertions that must be dropped. |
| 2026-09-03 | Process | Standardised commit messages via `.gitmessage` (Conventional Commits subject + **User Requests** / **Changes** / **Technical Details** sections). Requires two local settings: `git config --local commit.template .gitmessage` and `git config --local core.commentChar ';'`. | The **User Requests** section preserves the intent behind each change — in AI-assisted work the originating ask, and especially the mid-course correction, is the context most easily lost. The pre-commit checklist enforces the living-docs discipline at the point where drift would otherwise be introduced. |
| 2026-09-03 | CLI | Full command tree declared in `cli.py`, matching the surface already documented in `user-guide.md`. Stage commands are scaffolded but **fail loudly** — each raises a `ClickException` naming its implementing phase rather than being omitted or no-op'ing. New `docushift doctor` (paths + artifact presence) is the only functional command in Phase 1; `status` stays reserved for the Phase 7 delta dashboard the user guide describes. | `[project.scripts]` already promised `docushift.cli:main`, so the module had to exist. A `convert` that exits 0 while converting nothing is worse than one that doesn't exist: it makes the pipeline look further along than it is — the same failure mode the Phase 1 audit found in `pytest` exiting green on zero tests. |
| 2026-09-03 | Dependencies | Added `jinja2` to `pyproject.toml` dependencies. | `config/aem_templates/` ships Jinja templates for the Stage 6 synthesizer; a template directory with no template engine behind it is incoherent. |
| 2026-09-03 | Tooling | Added `[tool.ruff]` to `pyproject.toml` with an explicitly pinned rule set (`E,F,I,UP,B,SIM`, line length 120). Fixed the 47 findings this surfaced in the pre-existing `models.py` / `config.py` / `catalog.py`, including two unused imports, an unused variable, and `str, Enum` → `StrEnum`. | `ruff` was a declared dev dependency with no configuration, so its rule set was whatever the installed version defaulted to — the lint result would have silently changed on upgrade. All 47 findings were in code written before the environment existed; none were in the Phase 1 additions, which is what running the linter for the first time was meant to establish. |
| 2026-09-03 | Merge Implementation | The 3-way decision reduces to one predicate: take the fetched value only if the CSV value still equals the recorded snapshot (`CatalogManager._take_theirs`). With **no** snapshot, the fallback is to keep the CSV value — except where it is empty. | Field-by-field, not row-by-row: an edited `display_name` must not freeze the `slug` next to it. The no-snapshot fallback is the conservative direction — inventing a base would silently overwrite edits made before the state DB existed. |
| 2026-09-03 | Merge Scope | Discovery owns only `display_name`, `slug`, `is_archived`, `convert_eligible`, `release_date`, `zip_url`. `engine`/`engine_source` are excluded from the merge **and from `version_snapshot` entirely**; `family` is merged by provenance rank, not by snapshot comparison. | The engine columns are written by the detector after extraction, so a fetch has nothing true to say about them — omitting the columns from the snapshot table makes that structural rather than a rule someone can forget. Family needs ranking because two automated sources disagree with different confidence. |
| 2026-09-03 | Merge Safety | Deletion detection is scoped to the products present in the current fetch. | Otherwise `catalog fetch --product ems` would read every other product's absence as a deletion and abort — or, with `--allow-deletes`, wipe the catalog. |
| 2026-09-03 | Testing | `tests/` became a package (`__init__.py` at each level) and `pythonpath` gained `"."`, so suites can share `make_product` / `make_version` builders from `tests.conftest`. | Merge tests need to construct discovery-shaped products inline; fixtures cannot be called with arguments, and duplicating the builders across `test_catalog.py`, `test_state.py`, and `test_cli.py` would let them drift apart. |
| 2026-09-03 | Taxonomy | Docsite category data (`/product/categories`, `/api/bu_category_products`) is **advisory only, never authoritative**. Added `family_source` provenance column (`manual` > `taxonomy_rule` > `docsite_category` > `unclassified`, first wins). | Confirmed by user: the majority of products carry no docsite category, so classification is a majority-manual triage job. Provenance distinguishes "triaged and genuinely general" from "never looked at" and makes triage progress a reportable metric. |
| 2026-09-03 | Selection | New **`convert_batch`** column in `versions.csv` — a free-text run label (`poc-1`, `wave-2`), empty by default, **opt-in**. Kept separate from `convert_eligible`; the two compose and eligibility is the hard gate. `--batch` is a selector on `catalog fetch/list`, `download`, `extract`, `convert`, `sync`, plus `catalog batches` and `catalog set --batch`. | Confirmed by user: conversion is frequently partial (POCs, waves). With only `convert_eligible` — which defaults to `true` — scoping a three-version POC means flipping ~1,497 rows to `false`, and "deliberately out of scope" becomes indistinguishable from "not in this wave". Opt-in inverts the cost: three cells, not the whole sheet. Structurally excluded from `version_snapshot` for the same reason as the engine columns — no automated stage writes it, so a fetch has nothing true to say about it. |
| 2026-09-03 | Workspace | Downloads and extractions move out of `cache/{downloads,extracted}/` into a per-family workspace: **`families/{locale}-{bu}-{family}/{downloads,extracted,archive}/`**. `ConfigManager` is the sole owner of the contract (`family_dir`, `downloads_dir`, `extracted_dir`, `archive_dir`, `download_path`, `extract_path`); `utils/slug.py` is the sole owner of the name. Per-family folders are created on demand, not at startup. | Requested by user. The name matches the predecessor `html-to-md` project, where `en-us-<bu>-<family>` doubles as the **publishing repository name** — so the working tree already mirrors its destination. `downloads/` and `extracted/` split at the top of the family so reclaiming disk after an extract is one `rmtree`, not a glob. Versions deliberately keep their dots (`10.4.0`, not `10-4-0`): this path is keyed by the catalog and must round-trip to a `versions.csv` key, which `6-2-3` cannot. Dots-to-dashes is a Stage 6 (publishing path) concern. Trademark symbols are stripped *before* the NFKD fold, since NFKD decomposes `™` into the literal letters "TM". |
| 2026-09-03 | Archive Policy | Archived versions are **never downloaded and never extracted** by the pipeline — extraction now runs over the same selection as download. On-demand access is a separate utility: `docushift archive list` / `docushift archive download [--extract]`, writing to `families/<family>/archive/`. | User chose the separate-utility option over the presets offered. Closes an asymmetry the audit found: `convert_eligible` was documented as the *download* filter with nothing said about extraction. `archive/` sits outside `downloads/` deliberately — a reference ZIP in the pipeline's working set would look to Stage 4 like a package awaiting conversion. |
| 2026-09-03 | Package Source | **Manually supplied ZIPs are a first-class package source.** New `zip_source` column in `versions.csv` (`auto` \| `manual`); `docushift download --from-file <zip>` and `docushift archive download --from-file <zip>` copy a hand-obtained package to the canonical `download_path()` / new `archive_path()`, validate it with `zipfile.is_zipfile`, and pin the row. `validate()` exempts `manual` rows from the *convert-eligible with no `zip_url`* problem; `zip_source` joins the structural merge exclusions. Design only — see `architecture.md` §3.8. | Requested by user: discovery will not always resolve a usable `zip_url` (products with no download-all bundle, non-composable `folder_path`, stale archive `zipPath`), and the ZIP is often obtainable another way. The file goes to the location the pipeline already reads, so Stage 4 needs no second code path. The provenance must be a **CSV column, not a filesystem check**: `families/` is git-ignored, so a validator that stats disk would call every hand-supplied row broken on any machine that has not downloaded yet. The local path is deliberately **not** stored — `D:\downloads\x.zip` is valid on one machine, whereas `(bu, family, product_code, version)` derives the location anywhere. `zip_url` stays merged while only the supply decision is pinned, which makes "discovery has since found a real URL" a computable warning. |
| 2026-09-03 | Process | Implementation order returns to **Phase 3 (discovery crawler)**; the Phase 4 work already landed is design-only (a CSV column and a path contract, no network dependency). The `zip_source` schema/merge/validate half is filed under Phase 3, not Phase 4. | User flagged that Phase 4 implementation had run ahead of Phase 3. The `zip_source` split is not arbitrary: the merge exclusion has to exist *before* the first real `catalog fetch`, or a fetch could overwrite a hand-supplied row. |
| 2026-09-03 | Discovery | **The live API is not shaped the way the endpoint names imply**, verified against `tibco-enterprise-message-service` (`architecture.md` §2.1). Every response is wrapped in `{"result": {…}}`; `/api/products/{slug}` returns the *current version as the product object itself* with the rest under `siblings`; `isArchive` splits active from archived per record; the archive index **overlaps** siblings rather than replacing them. | Each of these would have failed silently rather than loudly. Reading only `siblings` drops the newest version of every product; ignoring the envelope yields an empty catalog that looks like a successful crawl. The crawler is now written against the observed payloads and the assumptions are recorded in the doc, so the next schema drift is a diff against something written down rather than a rediscovery. |
| 2026-09-03 | Discovery | **A ZIP URL is only templated for active versions whose `folder_path` is version-shaped (`<code>/<version>`); archived versions take the archive index's `zipPath` verbatim, or nothing.** `published_date` is excluded from the release-date candidates in favour of the archive index's `GA_date`. | Archived siblings carry stale folder paths (`enterprise_message_service`, `ems-zlinux`), so templating from them produces a plausible URL that 404s at download time — recorded in the catalog as fact, discovered three phases later. `published_date` on old releases is a bulk-migration timestamp: every EMS 5.x and 6.x record reads `2022-05-26`. Verified: 33 EMS versions, 4 spot-checked URLs all HTTP 200. |
| 2026-09-03 | Discovery | **70 of the 739 A-to-Z entries are `isPublicLevel: false` and are filtered out before the request**; a missing flag is treated as public. A sign-in page arriving as HTTP 200 also gets its own error message. | Those products answer with an SSO interstitial served as HTTP 200 — 3 of the first 13 sampled products, which read as crawler bugs. Filtering on the flag the docsite already publishes is cheaper and more honest than sniffing HTML; defaulting a *missing* flag to public means a schema change degrades to noise rather than silently emptying the crawl. Post-filter sample crawl: 0 errors. |
| 2026-09-03 | Discovery | **A product whose fetch fails is excluded from `CrawlResult.products` entirely, never returned with an empty version list.** `catalog fetch` requires a scope, and `--product`/`--batch` resolve to selectors matching either the docsite slug or the catalog code. | An empty version list reads to the 3-way merge as "every version was deleted upstream" — a transient 503 would abort the fetch or, with `--allow-deletes`, wipe the product. On scoping: an unscoped crawl is 668 requests, and a product's code is rarely its slug (`ems` is published as `tibco-enterprise-message-service`), so the selector set carries both. |
| 2026-09-03 | Taxonomy | A `family` value not declared in `taxonomy.yaml` is **auto-registered with a warning**, never rejected. `CatalogManager.warnings()` is separate from `validate()`: problems block the write, warnings do not. The warning names the folder that will be created. | User chose warn-once over rejection: families are user-extensible by design, and a hard failure would make `catalog import` unusable mid-triage. Naming the resulting `families/en-us-tibco-streaming-analytics` is what makes the warning also catch the other case — a typo (`mesaging`) about to become its own folder. |
| 2026-09-04 | CSH | **Context-sensitive help becomes a first-class conversion output**: one `csh.yml` per product version at the Markdown output root (beside `toc.yml`), plus a `csh:` list in each mapped topic's frontmatter. Full design in `architecture.md` §5.4. Design only — no implementation. | Requested by user. A product's Help button passes an identifier, not a URL; if the identifier does not survive migration the button breaks in the shipping product and nothing in the Markdown looks wrong. Grounded in a survey of **272 real `Alias.xml` files / 7,220 `<Map>` entries** in the predecessor `html-to-md` cache rather than in the format's documentation. |
| 2026-09-04 | CSH | **The map is keyed by `Name`, never by `ResolvedId`.** ~~`ids` survives only as a derived index, nested per doc-set, whose values are always lists.~~ *(the derived `ids` index was dropped entirely 2026-09-07 — see below; the name-as-key half stands)* | `ResolvedId` is non-unique **within a single alias file** in 29 of 196 files (15%), and the colliding ids address different topics — BW 6.12.0 `bwce-html` reuses 18. `Name` had 0 violations across the whole corpus. The predecessor's `csh_map.json` was keyed by id, so those entries silently overwrote each other. Nesting `ids` per doc-set matches the real scope of an integer id: it is per help target, not per version. |
| 2026-09-04 | CSH | **Identifiers are compared and stored byte-exactly; nothing case-folds them.** Names are emitted double-quoted in YAML, ids as bare integers. | Confirmed by user and by the corpus: TIBCO BC 7.4/7.5 ship `GatewayInstances` → `Gateway_Instances.htm` and `gatewayInstances` → `Managing_Gateway_Instances.htm` as two live, different help targets. Quoting is not cosmetic — an unquoted name of `1000`, `Yes`, `On`, or `6.2` loads as an int/bool/float under a YAML 1.1 loader such as PyYAML, which is what the map is read with. |
| 2026-09-04 | CSH | **Resolution is doc-set first, then version-wide**, merging identifiers across a version's several help outputs. Conflicts keep the largest doc-set as primary (ties alphabetical) and record the alternative under `also:`; nothing is dropped. | 15 of 254 versions ship more than one alias file, and Flare frequently copies one output's alias file wholesale into a sibling: **1,609 of 7,220 links (22%) dangle**, with 10 of the 11 affected files being `relnotes/Data/Alias.xml` at 0% resolution. Per-doc-set resolution would report 205 broken identifiers for BW relnotes; version-wide resolution resolves all 205 against the main output where the topics actually live. This is the strongest argument for the per-version file the user asked for. Ids collide across doc-sets (31 of 205 in BW 6.12.0) and names occasionally do (5 of 233), so the conflict case has to be representable rather than assumed away. |
| 2026-09-04 | CSH | **Absent CSH is normal, not a failure.** Empty, zero-byte, and unparseable sources are counted and skipped; a version with no identifiers gets **no `csh.yml` at all**. Unresolvable identifiers are kept in an `unresolved:` list rather than dropped. | 445 empty `<CatapultAliasFile />` plus 31 zero-byte files — 55% of the corpus (re-measured 2026-09-07; the original subset read 28%). Raising on those would make a routine condition look like a defect. Conversely an empty map file is indistinguishable from a failed run, so absence is the honest signal. Keeping unresolved entries is the mirror-image concern: a dropped identifier converts a broken Help button into a silent absence, which no downstream check can find. |
| 2026-09-04 | CSH | **CSH is engine-neutral**: `transforms/csh.py` owns the schema, resolver and writer; each engine contributes only a reader. ~~DITA yields `id: null` as a real state.~~ *(moot from 2026-09-07 — there is no id field; DITA's name-only contract is simply the general case)* | Verified against the predecessor's three separate working implementations (Flare `Alias.xml`, WebWorks `ctx/` + `wwhdata/xml/files.xml`, DITA `head.js` `suitehelp.contexts`), which had diverged into three schemas and three frontmatter conventions for the same concept. |
| 2026-09-04 | CSH | **No backward-compatible `csh_map.json` is emitted.** Confirmed by user: no AEM-side consumer is built yet, so `csh.yml` is free-standing and nothing constrains its filename or key. | Worth recording as a decision rather than an omission — the predecessor's id-keyed `csh_map.json` is the obvious thing to keep emitting "just in case," and doing so would carry its data loss forward into a consumer that does not exist yet. If a contract appears later it can be generated from `csh.yml`, which is lossless; the reverse is not true. |
| 2026-09-04 | CSH | ~~Frontmatter uses a single `csh: [{name, id}]` list~~ *(superseded 2026-09-07: with no numeric key there is nothing to pair, so frontmatter is a flat `csh: ["id-a", "id-b"]`)*, **not** the predecessor's parallel `csh_ids` / `csh_names` arrays. Confirmed by user 2026-09-04. Identifiers are resolved before the topic is written, so frontmatter lands in the file's first and only write. | A page commonly owns several identifiers, and parallel arrays lose the pairing the moment the counts differ — which they always do for DITA, where there are names and no ids. Writing frontmatter in a second read-modify-write pass over every converted file (what the predecessor did) re-parses and rewrites the whole output tree to touch a small minority of it. |
| 2026-09-07 | CSH | **Numeric identifiers are removed from the CSH schema entirely.** Flare's `ResolvedId` is read and discarded; `csh.yml` has one index (`topics`) keyed by a single string identifier, and frontmatter becomes a flat `csh: ["id-a", "id-b"]`. | Called by user: an identifier that is not unique is not an identifier, so carrying it as a second key means carrying a key that is wrong in 15% of alias files. The simplification is real — one index instead of two, no derived-index drift to reason about, no `{name, id}` pairing in frontmatter. Reversible if needed: `Alias.xml` stays in the extracted tree and `csh.yml` is a build artifact, so reintroducing a numeric index costs a re-run rather than a migration. |
| 2026-09-07 | CSH | **The identifier is a string, not a name — and WebWorks is why.** Flare contributes its alias `Name`, DITA its context name, WebWorks the **ctx file stem** (`admin1234`). One namespace, one type. | Flagged during the above change: WebWorks help has no alias name at all. The application requests `ctx/admin1234.htm` directly, so the stem *is* the shipped key and the internal topic id from `files.xml` is invisible to the product. A literally name-only schema would have dropped WebWorks CSH in full. The consequence is that digit-only identifiers are routine, which promotes the YAML quoting rule from a nicety to a correctness requirement — unquoted, `1234` loads as an int and `6.2` as a float, in a map documented as string-keyed. Case-sensitivity likewise becomes load-bearing: with the integer gone, byte-exact comparison is the only thing separating `GatewayInstances` from `gatewayInstances`. |
| 2026-09-07 | Docs | **New fourth living document: `docs/design.md`** — the tool's logic and algorithms in standard English, step by step, with every algorithm marked **Built** (implemented and tested) or **Specified** (designed, not yet written) and an index table mapping each to its module or its phase. `architecture.md` keeps the shapes and the rationale; `design.md` keeps the procedures. | Requested by user. The algorithms existed only as prose scattered through `architecture.md`, as module docstrings, and as the code itself — so the ordering rules that carry the actual data-safety guarantees (trademark strip before NFKD fold, deletion check before any write, resolve doc-set before version-wide) were discoverable only by reading three places at once. The Built/Specified split is what keeps the document honest as Phases 4-7 land: an unbuilt algorithm is stated as a binding contract on the implementation, not described as if it already runs. |
| 2026-09-07 | Sync | **The Stage 7 publishing layout is settled** — `{locale}-{bu}-{family}/{locale}/{product}/{doc-class}/{version-dashed}/`, the predecessor's publishing form, over the nested `{bu}/{family}/{product}/{version}/` form. The docs repo takes `online-help`, `user-guides`, `release-information` and `reference-documents`; the sibling `-resources` repo keeps only `api-references` and `archives`. Design only — see `architecture.md` §6.1-6.2. | Requested by user, who moved the three asset doc-classes out of `-resources`. The criterion is what the artefact *is*, not whether it is Markdown: the docs repo holds the per-version publication set — everything a human authored and a reader opens, PDF included — so one release's documentation is reviewable in one repository. `-resources` keeps the two classes that are neither authored nor read as prose: generated API trees, which are bulk and regenerate wholesale, and archived ZIPs. This also matches what the predecessor's code actually does: `html-to-md`'s `tibco_restructure.py` in `--phase-group` mode already sets `assets_dst` to the main group and reserves `-resources` for api-refs plus archives; its `CLAUDE.md` table describing otherwise is stale. Two consequences land on other stages: Javadoc must be **excluded from Stage 5 conversion and copied verbatim** (it is not engine output — converting it breaks working HTML), and Stage 7 must rewrite help-to-API links that now cross a repository boundary. |
| 2026-09-07 | Sync | **`-resources` stays a separate repository** per family, not a directory inside the docs repo. ~~Stage 7 therefore creates and pushes two repositories per family.~~ *(corrected 2026-09-09: Stage 7 writes two repo-shaped **trees** per family and publishes neither — see the scope row below; the separation itself stands unchanged.)* See `architecture.md` §6.3. | Confirmed by user. Javadoc trees run to thousands of generated files and `archives/` accumulates every ZIP a product ever shipped; both grow monotonically and neither deltas, so inside the docs repo they would dominate its history permanently and be paid for by every author cloning to fix a typo. They also have a different lifecycle — regenerated wholesale, or append-only — and want none of the docs repo's review workflow: a 4,000-file Javadoc diff makes the prose changes beside it unfindable. Matches the predecessor, which already publishes `<name>-resources` siblings. **Consequence:** a cross-repo link cannot assume a relative path — `html-to-md`'s `../../`-prefix rewrite works only because both trees sit under one local `output/`. The link *encoding* (relative sibling, absolute URL, or AEM-resolved reference) is the one item still pinned to a real AEM target; the rewriter's matching and destination are fixed. |
| 2026-09-07 | Sync | **Cross-repo links are absolute URLs.** Help-topic links into `api-references/` are rewritten to `{publish_base_url}/{locale}-{bu}-{family}-resources/{locale}/{product}/api-references/{subdir}/{version-dashed}/…`; links that stay inside the docs repo remain relative. New **`config/publishing.yaml`** holds `publish_base_url` and the doc-class-to-repo map. See `architecture.md` §6.4. | Confirmed by user: AEM serves the two repositories at content paths it decides, so no filesystem offset between them is guaranteed and the predecessor's relative `../../` prefix cannot carry over. The base is configuration rather than a constant so a staging target is a config edit, not a code change. One path-template function serves both the file copy and the link rewrite, which is what makes it impossible for a link to point where the copy did not write. **Consequence for Phase 7:** the link checker must classify before it checks — absolute URLs are external and skipped by default, HTTP-checked only behind a flag, or it would report every product's entire API surface as broken. Keeping intra-repo links relative is deliberate: it is what lets the docs repo be previewed and reviewed before publication. |
| 2026-09-09 | Scope | **Git sync mechanics are out of scope for this tool.** `docushift sync` organizes the output into repo-shaped directory trees under `--target-dir` and stops: no `git init`/`commit`/`push`, no repository creation, no GitHub authentication, no branch/review/PR policy, and no `gitpython` or `gh` dependency. Publishing is picked up afterwards by whoever owns the repositories. `architecture.md` §6.0 (new), §6.3 retitled, `design.md` §10, `planning.md` Phase 6, `user-guide.md` §4, `sync/__init__.py`. | Requested by user, to keep the tool simple. The scope this removes was the largest genuinely unplanned area left in the roadmap — repository creation, branch strategy, commit granularity, auth, and re-run-against-a-diverged-branch semantics — and none of it has a defensible default: branch naming, review gates and release cadence are organizational decisions the tool would be guessing at. **Nothing about the layout changes**, which is what makes the cut clean: the two-tree split (§6.3), doc-class routing (§6.2) and absolute cross-tree links (§6.4) are all properties of *what gets written*, and the trees are named exactly as the repositories are, so the hand-off stays a copy rather than a translation. Two consequences worth keeping: the output is inspectable and diffable with ordinary file tools by a reviewer holding no credentials; and idempotency reduces to one filesystem question — a re-run must replace a version's doc-class folder wholesale, so a topic deleted upstream cannot survive as a stale file — instead of also spanning dirty working trees and force-pushes. §6.4's absolute URLs are unaffected and remain necessary: they were never a filesystem-offset workaround, but a consequence of AEM serving the two trees at content paths it decides. |
| 2026-09-09 | Scope | **61 EBX and Spotfire products are permanently out of scope for conversion.** Recorded as a rule in a new **`config/scope.yaml`** (slug + display name + reason), resolved at merge time into a new **`in_scope`** column in `products.csv` with **`scope_source`** provenance (`manual` > `scope_rule` > `default`), and enforced as the outermost gate in `iter_versions(eligible_only=True)`. **Matching is by exact docsite slug** — never substring, never display name. `architecture.md` §3.10 (new, with the verified slug list), §3.1/§3.5/§3.7 updated; `design.md` §3.3.1 (new), §3.5, §4, §8.2; `planning.md` Phase 3.5. | Supplied by user as the authoritative docsite list of 56, then extended by the user on review with five more — `spotfire-desktop`, `spotfire-server`, `spotfire-enterprise-runtime-for-r-server-edition`, `tibco-spotfire-for-apple-ipad`, `tibco-silver-fabric-enabler-for-spotfire-web-player`, 291 versions between them. All 61 verified 2026-09-09 against the live A-to-Z index — **61/61 matched 1:1, all public, 0 unmatched, 0 ambiguous**. A rule was needed rather than 61 edited rows for two reasons: both CSVs are currently **empty**, so there are no rows to edit; and even once populated, next quarter's release of an excluded product arrives from a fetch defaulting to `convert_eligible=true` — an exclusion expressed as cleared version flags decays silently. Substring matching was measured and rejected: `ebx` also matches `tibco-businessconnect-ebxml-protocol` and `tibco-businessconnect-container-edition-ebxml-protocol` (ebXML, an unrelated B2B standard), and `spotfire` also matches 16 public products that remain in scope, among them the whole Data Science and Statistica lines — the same lesson §3.9.1 records for `api-exchange-gateway/`. Display-name matching fails separately: the names arrive mojibaked (`TIBCO EBXÂ®`) and `slugify()` folds `™®©℠` away by design. **Excluded ≠ absent**: an out-of-scope product is still discovered, catalogued and counted, exactly as archived versions are (§4.3), so *deliberately excluded* stays distinguishable from *never seen*. Two reporting consequences: a rule matching no product is warned about, because an upstream rename is how an exclusion silently stops working; and `scope_source=manual` outranks the rule file, so one product can be readmitted by a CSV edit that no later fetch undoes. Nineteen public products with a matching-looking slug **stay in scope** and are named in §3.10 so their absence reads as a decision — including `tibco-product-and-service-catalog-powered-by-tibco-ebx`, built *on* EBX but a distinct product; `spotfire-analytics` and `tibco-spotfire-advanced-data-services` need no rule at all because discovery already filters non-public entries. |
| 2026-09-09 | Engines | **The file-named DITA flavour is out of scope, and the DocBook engine is narrowed to two products.** File-named DITA leaves Phase 5 as a build item; `engines/dita.py` keeps the flavour branch and its report line as a *guard*, not a deferral. DocBook stops being "a placeholder line, not a design" and becomes a scoped, measured planning item for **`str` (TIBCO Streaming, 6 versions)** and **`sfire-sfds` (Spotfire Data Streams, 4 versions)** — a survey and an `architecture.md` §5.x still owed before any code. `architecture.md` §3.4, §5.2.1; `design.md` §7.2; `planning.md` Phase 5. | Both follow from the 61-product exclusion, and both were measured before being written down (`C:\tmp\sc7_flavours.py`, one walk of the whole cache). File-named DITA: 9 products publish it — `sf-pysrv` 27 versions, `sf-rsrv` 21, `enterprise-runtime-for-R` 11, `ebx-addon` 2, plus `sf_ipad`, `sf_ipad_deploykit`, `sfire-android`, `sfire-cloud`, `sfire_dev` — and **all 9 are on the exclusion list**, checked slug by slug. So it is not Spotfire-only as §5.2.1 said (one EBX add-on ships it) and it now has no in-scope population at all. DocBook: **exactly 10 versions in exactly 2 products**, which is §3.4's sweep count to the version — 38,178 HTML files, 865 MB, the largest per-version packages in the corpus, of which a coarse probe puts ~63% under `api/`-style trees, so the prose population is on the order of 14,000 topics and §6.3's predicate has to settle it before the engine can be sized. **`spotfire-data-streams` is in scope only because the rule matches exact slugs** — the substring `spotfire` rule rejected above would have taken it and left DocBook with one product, which is the clearest payoff yet from that decision. What stays despite having zero live yield: the `DC.*` clause and its case-insensitivity (`design.md` §7.2), because an out-of-scope product is never extracted so detection never runs on one — the rule costs one `re.I` and is what makes a readmitted product detect as `dita` instead of `auto`. §5.2.1's measurements stay on the page for the same reason: scope is a policy file, and policy changes. |
| 2026-09-10 | Catalog Key | **`slug` replaces `product_code` as the catalog's primary key**, in `products.csv`, the `versions.csv` join, all six `state.db` primary keys, and the workspace and publishing path segments. `product_code` is kept as a short descriptive column and is no longer required to be unique. `validate()` gains a duplicate-slug check. `planning.md` Phase 3.6. | Forced by the first full `catalog fetch --all` (2026-09-09), which **aborted before writing anything**: the deletion guard refused a merge that would have dropped 38 version rows. The guard was right and its diagnostic was wrong — the cause was not a mangled version key but a **collision in `product_code`**, which `_derive_code` takes from the first segment of `folder_path`. Rebranded products keep both their old and new docsite entries listed and both publish into the same folder, so **10 codes are shared by 21 products** — `spotfire` by three, `loglmi` by `tibco-loglogic` (1 version) and `tibco-loglogic-log-management-intelligence` (20), and eight more. The abort was the mild symptom: with the guard bypassed, two products collapse into one row carrying **one** slug, and `_resolve_scope` matches on slug. **`stat-sts` straddles the scope boundary** — `spotfire-service-for-statistica` is excluded, `tibco-data-science-service-for-tibco-spotfire` is not — so merge order alone decides whether 4 versions convert that never should or 5 are dropped that should not be. Phase 3.5 was being undermined one layer beneath itself. Slug is unique by construction (634 products, **0 duplicates, 0 nulls**), stable across rebrands, and already the key `scope.yaml` matches on, so this collapses the catalog key and the scope key into one thing that cannot disagree. Disambiguating the code was rejected: it is smaller, but the tie-break would hinge on version counts, and a code that shifts between fetches reads to the snapshot merge as a new product — a loud failure traded for a quiet one. The path cost is accepted deliberately: **9 of the 10 collisions also share `bu`+`family`**, so a code-named directory collides in the workspace and in the published tree too, and the slug segment is median 39 characters, max 95. Timing is the one piece of luck — both CSVs and `state.db` are still absent, so there is no migration and no user edit to preserve. |
| 2026-09-10 | End-of-Support | **A version support has retired is never converted.** Support's report ships as a committed input (`config/eos/EOS-Report-2026-09-10.csv`, 5,948 rows / 528 product names) behind a small pointer file (`config/eos.yaml`) holding the path and **14 reviewed name aliases**. `catalog eos` resolves it onto three new `versions.csv` columns — **`release_status`**, **`retirement_date`**, **`release_status_source`** (`manual` > `eos_report` > `unknown`) — and `retired` becomes the **second of four gates** in `iter_versions(eligible_only=True)`, inside `in_scope` and outside `convert_eligible`/`convert_batch`. `architecture.md` §3.11 (new), §3.2/§3.7 updated; `design.md` §1.1/§3.3.2/§3.5/§4/§11/§12; `user-guide.md`; `planning.md` Phase 3.7. | Measured against the real catalog on 2026-09-10: **128 in-scope eligible versions retired**, **11 products left with nothing to convert** (25 versions, all named on their own report line rather than buried in a warning), coverage **251 of 634** products, **0 stale aliases**. Three things the shape had to get right. **The report is re-applied every fetch, not applied once**: a retirement expressed as a cleared `convert_eligible` decays exactly as §3.10's scope exclusions would, because the next fetch defaults new rows to eligible — so this mirrors `scope.yaml`, tool-owned column with provenance, and step 3 *resets* so that deleting a row or an alias restores the version. **Only `Retired` gates.** `Retirement Announced` is a dated warning about a version still supported today; treating it as retired would drop **94** eligible versions retiring 2027-04-30 or later. Both are recorded, so a later report promotes them with no re-crawl. **Absence is silence, not a verdict** — 2,506 versions carry no row, **2,050** because their product is absent from the report entirely; the remaining 456 are mostly *newer* than anything the report lists for that product. That is why `catalog eos` prints coverage next to its result: "nothing retired" over 251 covered products is an answer, over 3 it is a broken join. The join itself is the risk, since the report offers no slug and no code — display name only. `slugify()` matches 237 names exactly; a prefix-tolerant pass proposes 20 more, and **version-set overlap rejects 6 of them**, including **`Spotfire Analytics` → `tibco-analytics`, which shares zero versions** (report 14.0.7–14.8.0 against catalog 7.x/10.x) — produced by stripping `spotfire-` from one side and `tibco-` from the other, the same looseness §3.10 refused for scope rules. Overlap *proves* an alias but does not disprove one — 13 exact-slug matches also share no version — so the file carries only the 14 provable aliases; the totals are identical either way, because a zero-overlap alias retires nothing by construction. Automatic fuzzy matching was rejected on the same evidence: no review step, and it is how a live product gets retired the day either side gains a version. Version matching is **exact string equality** — trailing-`.0` coercion would have resolved **1** of the 456 misses in exchange for reintroducing the `1.10` → `1.1` hazard the CSV layer exists to prevent — and dates are parsed by a dedicated `%m-%d-%Y` reader rather than `csvio.normalize_date`, whose permissive list reads `03-04-2021` as 3 April instead of 4 March. **No schema change**: the three columns are structurally excluded from `version_snapshot`, so there is no migration and no `SCHEMA_VERSION` bump. A retired version stays fully catalogued and visible to reporting (`catalog list --retired`), and `release_status_source=manual` outranks the report, so a hand-edit converts one anyway — the retirement date is deliberately left standing beside it, because support really did retire it that day. |
| 2026-09-09 | Stage 6 | **`nav.yml` and `meta.yml` stay placeholders until AEM supplies the details.** `config/aem_templates/nav.yml.j2` and `meta.yml.j2` are marked as placeholders in the template files themselves (non-rendering Jinja comments); their rendered content is explicitly **not a contract** and `design.md` §8.4 asserts nothing about it beyond "exists and parses as YAML". `toc.yml` and `index.md` are unaffected. `design.md` §10, `planning.md` Phase 6. | Requested by user. Both files are Phase-1 scaffolding guesses that were never measured against anything — unlike `toc.yml`, whose shape the three node rules fix, and `index.md`, whose shape §10.5 fixes. The risk being avoided is the one this project keeps meeting: a guess that goes unmarked reads as a spec a week later, and the eventual real template gets retro-fitted to it instead of written from the details. Nothing is blocked in the meantime — the synthesizer's actual work is the node list, which `toc.yml` consumes — and one input is already grounded and collected regardless of what the template ends up doing with it: SuiteHelp's `GUID-*-homepage.html` carries `publication-title`, `release-version` and `release-date` in all 314 doc-sets that ship one. |

---

## 4. Active Workstreams & Next Steps

### Verified state as of 2026-09-03
Audited against the filesystem, not against checkboxes:
- [x] Verified `docs.tibco.com` API endpoints and active/archived version models
- [x] `pyproject.toml`, `config/taxonomy.yaml` created
- [x] **Phase 1 scaffolding closed**: all 9 subpackages created; `cli.py` command tree written (so `[project.scripts]` now resolves); `tests/` with `conftest.py`, unit and integration suites, and a documented `fixtures/` plan; `config/docsite.yaml` and `config/aem_templates/` (Jinja `toc.yml`/`nav.yml`/`meta.yml`/`index.md`) added; `.gitignore` extended to `output/`, `*.db`, `.venv/`, and build/test caches
- [x] **Environment created and code executed for the first time** — `.venv` on Python 3.13.7, `pip install -e ".[dev]"`, `ruff check src tests` clean, and the `docushift` console script runs. Setup from the repo root:
  ```
  python -m venv .venv && .venv/Scripts/python -m pip install -e ".[dev]" && .venv/Scripts/python -m pytest
  ```
- [x] **Phase 2 closed**: `models.py` reshaped (per-version engine, `family_source`, volatile fields evicted); `utils/csvio.py`, `state.py`, and a CSV-backed `catalog.py` with the snapshot 3-way merge written; `config.py` switched to `products_path`/`versions_path` and rule-driven classification; `taxonomy.yaml` reshaped to families + rules; `config/catalog.json` removed; the `catalog` CLI group wired up apart from `fetch`. **157/157 tests pass**, lint clean.
- [x] **Phase 4's two design halves landed early** (the halves that are decisions, not I/O): the `convert_batch` selection model and the families-workspace path contract. `utils/slug.py`, `ConfigManager` path methods, `CatalogManager.iter_versions(batch=…, eligible_only=…)` / `batches()` / `warnings()`, `--batch` on every stage command, `catalog batches`, and the `archive` group (`list` functional, `download` pending Phase 4 I/O). **203/203 tests pass**, `ruff check src tests` clean; the CLI was exercised end to end against a demo catalog in `C:\tmp\dsdemo`.

- [x] **Phase 3 closed**: `discovery/client.py` (endpoints, templates and politeness from `docsite.yaml`; urllib3 `Retry`; a hard minimum request interval) and `discovery/crawler.py`, written against the API's verified real shape rather than its documented one. `catalog fetch` is wired to `CatalogManager.merge_fetch_results()` with a required scope, `--dry-run`, `--allow-deletes`, and a merge-stats table; docsite ids and folder paths land in `state.db`, not the CSVs. The `zip_source` catalog half shipped with it. **256/256 tests pass** (38 of them discovery, all offline against a fake session), lint clean. Verified live: a scoped EMS fetch returns 33 versions with correct archive flags and dates, spot-checked ZIP URLs answer HTTP 200, and a 13-product sample crawl completes with 0 errors.

- [x] **Phase 3.5 closed** (2026-09-09): product scope exclusions, in place before the first full fetch. `config/scope.yaml` with the 61 verified EBX/Spotfire slugs and `ConfigManager.load_scope()` (duplicate slug raises); `in_scope`/`scope_source` in `products.csv`; `_resolve_scope` at merge time, applied to new and existing products alike and actively resetting a rule that has been deleted; the `iter_versions(eligible_only=True)` gate; `catalog set --in-scope/--out-of-scope`, `catalog list --out-of-scope`, scope counts in `catalog triage` and `catalog show`, and the excluded count in the fetch stats; two new `warnings()`. **327/327 tests pass**, lint clean. Two things the implementation settled that the spec had not: `in_scope` is the only boolean read with `parse_optional_bool`, because §3.6's permissive read maps a blank cell to `false` and a hand-added row would otherwise vanish from every stage; and unmatched rules are reported as **one aggregated line** computed over the whole catalog, since 61 rules against a partly fetched catalog would otherwise emit dozens of near-identical warnings.

- [x] **First full crawl run (2026-09-09/10)** — `catalog fetch --all --include-archived` against the live docsite. **634 products, 4,462 versions, 0 errors**, dumped to `C:\tmp\crawl_all.json` so the analysis below needed no further requests. The crawler is proven at full scale; the *merge* is what aborted (see Phase 3.6). Three numbers worth keeping: **60 of the 61 scope rules matched** a crawled product, the one miss being `tibco-spotfire-for-apple-ipad` — the rename detector firing on its first real run, and still to be chased; **845 versions** belong to excluded products; and **625 of 634 products have a `product_code` that differs from their slug**, which is what makes the code worth keeping as a column even after it stops being the key.

### Next steps

- [x] **Phase 3.6 closed** (2026-09-10): the catalog is re-keyed on `slug`. `Product.slug` is required and first in `products.csv`; `versions.csv` joins on it; all six `state.db` primary keys moved (schema v2, with a version guard that refuses a v1 file by name rather than failing on `no such column: slug` mid-fetch); `download_path`/`extract_path` take the slug; `CatalogManager.resolve_slug()` keeps `--product` accepting either spelling and **refuses an ambiguous code with the candidate slugs listed**; `validate()` reports a duplicate slug. `_derive_code` is untouched — it still answers what the docsite calls the ZIP folder, which was never the thing that was wrong. **349/349 tests pass**, lint clean. Two things the implementation added that the plan had not: the whole 634-product crawl is committed as a JSONL fixture (`tests/fixtures/discovery/`) so the re-merge regression runs offline; and `_record_snapshots` now runs in **one** `StateStore.transaction()`, which took that test from 116 s to 0.75 s — ~5,100 per-row commits — and removes the half-written merge base a mid-fetch crash used to leave behind
- [x] **First successful full fetch (2026-09-10)** — `catalog fetch --all --include-archived` on the re-keyed catalog: **634 products added, 4,462 versions added, 0 blocked deletions, 0 fields preserved** (nothing to preserve — the CSVs were empty), **60 products out of scope** covering **845 versions**, and 34 entries skipped for having no published versions plus 70 for not being public. `config/products.csv` and `config/versions.csv` exist for the first time and are tracked; `cache/state.db` holds the merge base. The 21 collision products all round-tripped as separate rows, and `stat-sts` came out split the way it should: `spotfire-service-for-statistica` `in_scope=false scope_source=scope_rule`, `tibco-data-science-service-for-tibco-spotfire` `true default`. A scoped re-fetch of the excluded one is a clean no-op, and `catalog import` reports no problems. Two known items carried forward, neither new: **303 of 634 products are `unclassified`** and need family triage, and the one scope rule `tibco-spotfire-for-apple-ipad` still matches nothing
- [x] **Phase 3.7 closed** (2026-09-10): a version support has retired is no longer converted. Support's report (`config/eos/EOS-Report-2026-09-10.csv`, 5,948 rows / 528 product names, self-consistent — 0 conflicting statuses) joins on **display name only**, so exact-slug matching is paired with the reviewed 14-alias `config/eos.yaml` rather than a fuzzy matcher — version-set overlap rejected 6 of 20 prefix-tolerant proposals, among them `Spotfire Analytics` → `tibco-analytics`, which shares **zero** versions with its supposed match. `release_status` / `retirement_date` / `release_status_source` join `versions.csv`, `retired` becomes the second of four `iter_versions` gates, `catalog eos` re-applies the report (and *resets*, so removing a row or an alias restores the version), `catalog list --retired` and `catalog set --release-status` expose it, and `catalog triage` reports it. **Absence never retires anything** — 2,506 versions have no row, **2,050** because the product is absent entirely. Run against the real catalog: **128 of 1,517 eligible versions retired**, **11 in-scope products left with nothing to convert** (25 versions — AuditSafe, BW ProcessMonitor, Data Science Team Studio, three Silver Fabric enablers and five more), named in full on their own report line, coverage **251 of 634**, 0 stale aliases. **404/404 tests pass** (+55), lint clean. No schema change — the three columns are structurally outside `version_snapshot`. Two survey figures were re-measured after the alias set was finalized and corrected in `planning.md`: absence-by-missing-product is 2,050 not 2,004, and trailing-`.0` coercion would have resolved **1** version rather than 14, which strengthens the case for exact matching
- [ ] **Phase 4 — acquisition I/O** against the settled contract: resumable downloader into `downloads_dir()`, ZIP extractor into `extract_path()`, `docushift archive download`, `--from-file` ingestion plus `ConfigManager.archive_path()`, and the per-version path/checksum/status writes into `state.db`
- [ ] **Phase 4 — the inventory measurement**: the counting walk, the marker-based `is_api_reference()` predicate, and the unmarked-candidate triage line in the `extract` report. The catalog half already landed (ahead of the extractor, the way `zip_source` did in Phase 3), so this is purely the Stage 4 side
- [ ] **Phase 5** — **MadCap Flare Engine** (specified 2026-09-08, `architecture.md` §5.1) + **SDL DITA Engine** (specified 2026-09-08, §5.2) + **WebWorks Engine** (specified 2026-09-09, §5.3) + CSH Mapper (**all three readers: Flare, DITA, WebWorks** — settled 2026-09-08) with `pytest` unit test suite. **All three converter engines are now measured against the corpus and none is implemented.** The fourth and last is **DocBook**, scoped 2026-09-09 to exactly two in-scope products — `str` (TIBCO Streaming, 6 versions) and `sfire-sfds` (Spotfire Data Streams, 4) — and still unsurveyed: pass 1 cannot see it at all, since its flat HTML has no layout marker and only the `DocBook XSL Stylesheets` generator comment identifies it (`design.md` §7.1). Ten versions is the smallest engine by count and not by bytes — 38,178 HTML files and 865 MB — so the survey's first job is the API/prose split. **The file-named DITA engine is no longer on this list**: all 9 products that publish it are out of scope (2026-09-09), so it left Phase 5 as a build item and survives only as a guard branch in `engines/dita.py`.

> **Standing docs obligation.** Every algorithm in `docs/design.md` is marked **Built** or **Specified** — or, since 2026-09-09, **Unsurveyed** (scope known, design not: the DocBook engine) or **Placeholder** (artifact written, shape still a guess: `nav.yml`, `meta.yml`). Landing one flips its mark and its row in the §12 index — in the same commit, not afterwards. A Specified algorithm that has quietly shipped is worse than an undocumented one: it reads as a plan while being the code.

### Design landed ahead of implementation (2026-09-04, simplified 2026-09-07)
- [x] **CSH system fully specified** — `architecture.md` §5.4, `user-guide.md` §7, planning tasks split across Phase 4 (source inventory) and Phase 5 (resolver, writer, frontmatter). Nothing implemented. **One string identifier, one index, no numeric key.** The spec is grounded in a survey of the predecessor `html-to-md` cache rather than in the Flare documentation:

  | Measured | Value |
  | :--- | :--- |
  | `Alias.xml` files inspected | 272 (196 with content, 65 empty, 11 zero-byte) |
  | `<Map>` entries | 7,220 across ~254 product versions |
  | Files where a `ResolvedId` addresses >1 topic | 29 of 196 (15%) |
  | Files where a `Name` addresses >1 topic | **0** |
  | Links that dangle inside their own doc-set | 1,609 of 7,220 (22%), 10 of 11 files being `relnotes` at 0% |
  | Versions shipping >1 alias file | 15 of 254 |
  | Links carrying a fragment | 239 |
  | Case-only name collisions with different targets | TIBCO BC 7.4 & 7.5 |

  The reproduction scripts live in `C:\tmp\an_alias*.py`; they are throwaway analysis, not part of the package. Re-running them needs the `html-to-md` cache, which is not in this repo.

- [x] **Extraction inventory columns — catalog half built** (2026-09-07) — `architecture.md` §3.9, `design.md` §6.3, `user-guide.md` §2, Phase 4 checklist. Five tool-owned columns in `versions.csv`, written by `docushift extract`: `_has_csh`, `_csh_names`, `_has_api_ref`, `_api_files`, `_doc_files`. Landed: the model fields, `VERSION_COLUMNS`, the blank-preserving `csvio` round-trip, merge/snapshot exclusion, `record_extract_inventory()` / `clear_extract_inventory()`, and the desync warning. **279/279 tests pass** (23 new), lint clean. Pending with the extractor: the counting walk and the API-reference predicate.

  The gap they close: every column the sheet carries today is discovery-time, so `convert_eligible` and `convert_batch` are set on packages nobody has opened. Four decisions worth keeping:

  - **They live in the CSV, not `state.db`, and that is a stated exception to §3.2.** The test §3.2 actually cares about is *does a fetch churn the diff* — these change only on `extract`. They earn the seat because they are the inputs to the two decisions the sheet exists to record, and a number you must query is a number nobody consults before tagging 200 rows into `wave-2`.
  - **Blank ≠ zero.** All five are optional in the model. Blank is "never extracted", `0` is "extracted, found none". Conflating them would make the archived half of the catalog indistinguishable from a corpus with no CSH in it. A failed extract writes nothing.
  - **`_has_csh` is not `_csh_names > 0`.** It records that a source *file* was found; the count records what parsed out of it. `true` with `0` is the empty-alias-file case — 55% of the surveyed corpus. `_has_api_ref` genuinely is a convenience duplicate of `_api_files > 0`; both are written by one call so they cannot drift, and `warnings()` flags a hand-edit that desyncs them.
  - **One shared `is_api_reference()` predicate.** Three stages need the same answer — Stage 4 counts, Stage 5 skips, Stage 7 routes — and three copies would let a file be counted as documentation and published as an API reference.

- [x] **API-reference predicate settled: markers decide, names raise a flag** (2026-09-07). Surveyed against the `html-to-md` extracted cache — **2,201,528 files, 515 products** (`design.md` §6.3.1; scripts `C:\tmp\an_segments*.py`). The seven-name list inherited from the predecessor is wrong in both directions:

  | Measured | Value |
  | :--- | :--- |
  | Files under the seven inherited segments | 283,267 |
  | Real API trees the list misses | `apidocs/` 27,450 · `apischemas/` 16,464 · `components-api/` 15,563 · `api-docs/` 3,935 · `api-reference/` 3,257 · `console-api/`+`config-api/` 3,930 · `cpp-reference/`+`c-and-cobol-reference/` 3,200 · `api reference/` 1,141 (with a space) |
  | Product names that a substring rule would destroy | `api-exchange-gateway/` 15,677 · `api-exchange-manager/` 1,015 |
  | Files wrongly skipped by the predecessor's `/c` substring rule | **186,856 (8.5%)** — including `csh.js` and the WebWorks `ctx/` CSH source directory |
  | Casings observed | `API`/`api`, `C`/`c`, `JavaDoc`/`javadoc`, `Java_API` |

  **Resolution:** a directory is an API-reference root iff it holds a known generator marker (`allclasses-frame.html`, `annotated.html`, `jsdoc-default.css`, `godocs.js`, `fti/FTI_*.json`, …); everything beneath inherits; the outermost match wins. **A name classifies nothing.** Names are used only to flag unmarked API-ish directories in the `extract` report, so an unseen generator becomes a question at extract time rather than broken Markdown at conversion time — and so `api-exchange-gateway/` can never be swept up. This is the conclusion §3.4 already reached for engine detection, for the same reason: what a tree contains is knowable, what someone named it is not.

  `javascript/` **is** an API tree (JSDoc, nested under `api-reference/`) — the earlier claim that it was documentation was wrong. `c-tutorial/` does not exist in the corpus.

- [x] ~~**CSH scope narrowed to MadCap Flare only**~~ **superseded 2026-09-08 — DITA is back in** (2026-09-07, user decision) — `architecture.md` §5.4.4, `design.md` §6.2/§9.2, `user-guide.md` §7, Phase 4/5 checklists. WebWorks and DITA CSH readers are **not built**. Their sources are still *located* by the Stage 4 inventory and named in the extract report as triage lines, so "we do not handle this version's help" stays a visible number rather than an assumption; they contribute nothing to `_has_csh` / `_csh_names`, which describe what will become a `csh.yml`.

  Two things this had to survive, since both were justified *by* WebWorks:

  - **The identifier stays a string.** Re-measured over the whole cache: **834 of 11,054 Flare names (7.5%) are digit-only** (`12`, `1000`, `1122`). The YAML double-quoting rule keeps its evidence without WebWorks. Measured precisely, the hazard is numeric coercion only — 0 names are `Yes`/`No`/`null`-shaped, 0 sexagesimal, 0 leading-zero — but quoting stays unconditional, because a conditional quote is a branch that can be wrong.
  - **Byte-exact comparison stays.** 8 case-only collisions in the full corpus, not just the one BC 7.4/7.5 anecdote.

  **Why Flare and not the others:** the Flare contract is the only one measured. The WebWorks and DITA contracts were transcribed from the predecessor's source and never checked — and checking the WebWorks one now weakens it: 692 `wwhdata/` trees against **69 `ctx/` directories**, so the input its reader is specified against is absent from 90% of WebWorks output. A `csh.yml` built on that is confidently wrong an unknown fraction of the time, and a help map pointing at the wrong page is worse than no help map: the first is invisible, the second is reportable.

- [x] **WebWorks CSH back in scope — all three HTML engines now read** (2026-09-08, user decision) — `architecture.md` §5.4/§5.4.3/§5.4.4, `design.md` §6.2/§9/§9.2/§9.3, `user-guide.md` §7, Phase 4/5 checklists. Scripts `C:	mpn_ww_csh.py`, `an_ww_csh2.py`. **This reverses the last remaining half of the 2026-09-07 narrowing, and it does so because the argument for it was wrong — not because the priority changed.**

  **The measurement error.** The stated reason WebWorks stayed out was that its reader depends on `ctx/`, and the cache holds 692 `wwhdata/` trees against only 69 `ctx/` directories — "absent from 90% of WebWorks output". `wwhdata/` is **per book**; `ctx/` is **per doc-set**; WebWorks averages **3.5 books per doc-set**. At matching granularity: 186 doc-sets hold WebWorks books and **68 of them have a `ctx/` (37%)**. The general form of the error is the part worth keeping — *a ratio between two counts is only evidence if the counts are of the same kind of thing* — because it is what produced two descoping decisions in one day.

  **And `ctx/` was never the source.** Those files are two-line redirect stubs (`document.location = "../index.htm?context=…&topic=…"`) generated *from* the map. The map is `<book>/wwhdata/common/topics.js`, a dispatch chain of `if(P=="<identifier>")C="<file>#<anchor>";`, present in **647 of 692 books (93%)**.

  **The measured contract**, and it is the best of the three:

  | | Flare | DITA (SDL) | WebWorks |
  | :--- | ---: | ---: | ---: |
  | Sources located | 863 | 418 | 647 |
  | …with at least one entry | 387 (45%) | 380 (91%) | 155 (24%) |
  | Entries | 11,054 | — | 3,439 |
  | Distinct identifiers | 2,396 | — | 1,180 |
  | Versions covered | ~254 | — | 101 |
  | Links resolving in their own source | 78% | — | **100%** |
  | Entries with a `#anchor` | 3% | ~0% | **43%** |

  All 3,439 targets exist and all 1,492 anchors are present in the file they name. Zero digit-only identifiers, zero case-only collisions — they are dotted lowercase names.

  Consequences beyond "build the reader":

  - **`wwhdata/xml/files.xml` is a trap.** It holds the same map in proper XML — more parseable than a JS dispatch chain — and agrees in 153 of 155 books. But both disagreements are entries *missing from the XML*, and one book ships no `files.xml` at all. `topics.js` is the source of record; the tidier format is the lossy one.
  - **The `anchor` field earns its keep here.** 43% of WebWorks entries carry one against Flare's 3%, so what looked like a Flare edge case is a WebWorks main path.
  - **The version-wide resolution fallback is Flare-specific.** It exists for the 22% of Flare links that dangle in their own doc-set; WebWorks never reaches it. The `also:` merge still applies — 26 identifiers are claimed by two books with different targets.
  - **The string/quoting rules lose one of their justifications and keep the other.** They are often attributed to WebWorks; WebWorks needs neither. Flare carries them alone (834 digit-only names, 8 case-only collisions). The rules stay unconditional anyway.

- [x] ~~**DITA CSH reader back in scope; WebWorks stays out**~~ **superseded the same day — WebWorks is in too** (2026-09-08, user decision) — `architecture.md` §5.4/§5.4.4, `design.md` §6.2/§9/§9.2, `user-guide.md` §7, Phase 4/5 checklists. **Reverses one half of the 2026-09-07 narrowing**, and the reversal is the point: that decision descoped DITA and WebWorks together on the shared ground that both contracts were transcribed from the predecessor's source and never checked. Checking DITA's held.

  **The measured DITA contract** (script `C:	mpn_headjs.py`, sample `activematrix-businessworks-plugin-for-marketo/7.1.0`): `static/head.js` assigns `suitehelp.contexts={"bwmarketo_palette":"GUID-3F0A….html", …}` — one flat JSON object, identifier → target HTML file, no nesting and no per-entry attributes. Present in **380 of 418 `head.js` files (91%)**; the other 38 assign an empty object, which is the DITA equivalent of Flare's empty `<CatapultAliasFile />` and is counted and skipped identically. It is a materially *simpler* read than `Alias.xml` — no `ResolvedId` to discard, no case-collision hazard from a second key.

  Two things this changes and one it does not:

  - **It matters at corpus scale.** DITA is 371 versions, the second-largest engine (above), so this is not a long-tail reader.
  - **`_has_csh` now means Flare *or* DITA.** The column still answers "will this version get a `csh.yml`?", so WebWorks continues to leave it `false` and produce a triage line.
  - **WebWorks is unaffected.** Its exclusion never rested on "unverified" — 692 `wwhdata/` trees against 69 `ctx/` directories is a measurement, and it says the input its reader needs is absent from 90% of WebWorks output.

  The correction worth remembering is procedural: I recommended descoping DITA on an argument I had not tested, and the user acted on it. Measuring first would have avoided a decision and its reversal.

- [x] **Engine detection extended to DITA and R help; unconvertible engines get real names** (2026-09-08, user decision) — `architecture.md` §3.4, `design.md` §7, Phase 5 checklist, `models.py`, `catalog.py`, `cli.py`. Scripts `C:	mpn_engine*.py`, `an_htmltype.py`, `an_rules.py`, `an_headjs.py`. The detection signals in `design.md` §7 had only ever been checked against one package (`dsp_gridserver` 7.1.1). Swept over all 1,822 cached versions:

  | Outcome | Versions | Note |
  | :--- | ---: | :--- |
  | No HTML at all | 539 (29.6%) | PDF-only packages — correctly nothing to detect |
  | Engine detected | 875 (48.0%) | flare 670, webworks 176, docbook 10, flare+webworks 19 — *the flare 670 is superseded; see the Flare converter-engine entry below* |
  | **HTML present, engine unknown** | **408 (22.4%)** | would be skipped silently |

  So detection covers **68% of the versions that actually have something to convert**, not the ~100% the single-sample check implied. Fingerprinting all 408 identifies almost all of them:

  | What it is | Versions | HTML files | Signal |
  | :--- | ---: | ---: | :--- |
  | **SDL DITA** | 371 (91%) | 61,712 | `GUID-*.html` filenames · `DC.Type`/`DC.Identifier`/`DC.Title` meta (**case-insensitive**) · `static/head.js`+`body.js` · `screen.css`+`print.css` |
  | **R help** (Rd → HTML) | 11 (3%) | 14,514 | `snext.css`/`snextchm.css` · `class="RdName"`/`"RdTitle"` |
  | RoboHelp 11 · FrontPage 6.0 · Help & Manual · MkDocs · Docusaurus · Maven Doxia | 19 | ~7,200 | `<meta name="generator">`, verbatim |
  | Hand-authored (`openspirit-*`) | 6 | ~10 | an author's name in a comment |

  Two rules — `GUID-*.html OR DC.* meta` and `snext.css OR Rd* class` — recover **382 of 408 (94%)**. Adding them plus the generator-meta pass would take coverage of HTML-bearing versions from 68% to **98%**.

  - **DITA is not a fringe engine — it is the second-largest, ahead of WebWorks** (371 versions vs 176). It went undetected because §7 looked for DITA-OT markers (`.dita` remnants, the DITA-OT generator comment) and this output comes from **SDL's publisher**, which emits neither. *(2026-09-08, later the same day: the 371 resolves into 316 SDL SuiteHelp + ~55 file-named Spotfire versions — two publishers, one `engine` value. See the converter-engine entry below.)*
  - **`<!-- NewPage -->` in `bex`/`spm` is Javadoc**, i.e. an API-reference tree rather than a missing engine. It belongs to the §6.3 predicate, not here.
  - The CSS/JS pair is the *broadest* signal (382) but the worst rule — `screen.css` + `print.css` is a filename coincidence waiting to happen. The narrower `GUID-*.html OR DC.* meta` is specified instead, even though it matches 11 fewer.

  **Decided:** both rules go into §7, plus a `<meta name="generator">` pass for the long tail. Coverage of HTML-bearing versions goes from 68% to **98%**.

  **And detecting is not converting — so the two must not share a value.** `SourceEngine` now carries `r-help`, `robohelp`, `frontpage`, `help-and-manual`, `mkdocs`, `docusaurus`, `doxia` and `other` beside the four convertible engines, with a `CONVERTIBLE_ENGINES` frozenset that Stage 5 tests membership of instead of asking "is it `auto`". Collapsing an identified generator into `auto` would have destroyed the distinction the column exists for: `auto` is a *detector bug*, a named engine with no handler is a *scoping decision*. `warnings()` names any `convert_eligible` row whose engine has no handler — such a row otherwise reads as completely settled and silently produces nothing — and `catalog set --engine` offers the full list, generated from the enum so the CLI cannot drift from the model. 18 new tests; **297/297 pass**, lint clean.

- [x] **The support and legal pages become the last two `toc.yml` nodes — and it is a move, not an append** (2026-09-09, user request) — `architecture.md` §5.1.5, `design.md` §10, Phase 5 and Phase 6 checklists. Scripts `C:\tmp\tp1_templates.py` … `tp7_h1.py`, all over the full 676-root Flare inventory.

  - **The request named three pages; the corpus has two.** "Third party notices page" and "legal … page" are one file — **0 of 676 roots ship a separate third-party page**, and the combined page has a single `h1` and **zero `h2` elements in all 669 copies**. The user confirmed the two-node reading. Worth recording because the alternative — splitting the page on a heading — would have been implementable and wrong, and only the `h2` count rules it out.
  - **The load-bearing finding is that these are already TOC entries** — legal in 657 roots (97%), support in 649 (96%). Read literally, "put them in the end of the toc.yml" is an append, and an append would have duplicated the topic in 96% of versions. The rule is the landing-page hoist inverted: relocate, never re-create.
  - **The rule mostly ratifies the source.** 610 of 676 roots (90.2%) already end support-then-legal; legal is last in 656 (97.0%). It earns its keep on the ~10% that deviate (20 support nodes in the middle, 13 in the first tenth, 4 first) and on 18 legal nodes nested one level down that need promoting.
  - **The TOC disambiguates multi-candidate roots, and a name heuristic would not.** 49 roots ship 2+ support pages and 5 ship 2+ legal pages; the TOC references exactly one in **all 54**. `fsp_transactioninsight/5.5.0` picks `Legal_and_Third-Party_Notices.htm` and `6.0.0` picks `Legal-and-Third-Party-Notices.htm` — same product, opposite filename conventions, so any priority order is wrong for half the corpus.
  - **Neither label may be hard-coded.** Support `h1` is `TIBCO …` 565, `ibi …` 49, `Spotfire …` 45, bare 4, malformed 5. A constant string would rebrand 96 roots. Nor is the content boilerplate: 588 distinct legal bodies and 595 distinct support bodies, so each version converts its own.
  - **The `_templates/` TOC figure was an order of magnitude short.** The first survey's *162 entries in 59 of 60 sampled roots* is, over all 676, **1,737 entries across 660 roots** — legal 657, support 649, what's new 381, home 32, other 18. Same shape, 10× the scale; corrected in `architecture.md` §5.1.5, §5.1.9 and Phase 5. The file census likewise: 2,797 files under 82 names (78 case-folded), not 2,803 under 80. Second time in two days that a 60-root sample has been re-measured against the full corpus and moved — the sample is for shape, the population is for numbers.

- [x] **`pypdf` adopted over `pymupdf` for PDF titles — verified, and the verification corrected the numbers it was checking** (2026-09-09, user request) — `architecture.md` §6.2.2, `design.md` §10.5, Phase 6 checklist. Scripts `C:\tmp\dc6_pypdf.py` (300-file replication), `dc7_pypdf_full.py` (head to head over all 5,007), `dc8_junk.py`. `pypdf` **not** added to `pyproject.toml` yet — nothing imports it until Stage 6 exists, and an unused dependency is not an adoption.

  - **The two libraries agree exactly.** Over all 5,007 de-duplicated user-guides PDFs: **byte-identical `/Title` on all 5,006 files both could read (100.00%)**, identical usable/blank/junk tallies, and pypdf **4× faster** (122 s against 501 s). pypdf 6.18.0 is BSD-3-Clause; pymupdf 1.27.2.2 is "AGPL 3.0 or Artifex Commercial". The licence question was the whole reason to check, and there is no quality trade-off to weigh against it.
  - **One file separates them**, and it argues for wrapping rather than against pypdf: `TIB_mftcc_8.4.4_user_guide.pdf` is truncated and raises `PdfStreamError` where MuPDF's more forgiving parser recovers a title. A pypdf exception now falls through to the filename fallback exactly as a blank does.
  - **Running the full population instead of re-running the sample is what caught the real error.** The shipped figure of **78% usable / 22% blank / 0% junk** came from 300 files; over 5,007 it is **70.8% / 27.9% / 1.3%**. The sample overstated usable by 7 points and, worse, reported **zero junk when there are 65** — so the spec as committed in `bc011ab` said no junk filter was needed. Corrected everywhere it appeared. The lesson is narrow and repeatable: a 6% sample is fine for *is this signal usable at all*, and not fine for *does this failure mode exist*, because a 1.3% class is likely to be absent from it entirely.
  - **The junk turned out to be one closed family, so the filter is short.** All 65 are the authoring tool's source filename in 37 distinct values — 22 `untitled`/`Untitled`, 24 ending `.book` (FrameMaker), 8 ending `.fm`, the rest `Microsoft Word - TIB_silver_mobile_install_guide.docx` and kin. Reject those three shapes; 0 titles are suspiciously short, so nothing else needs a rule.

- [x] **`archives/` gets an `index.md` and a `toc.yml`, built from the catalog rather than the directory** (2026-09-09, user decision) — `architecture.md` §6.1/§6.2/§6.2.3, `design.md` §10/§10.6 (+§12 row), Phase 6 checklist, `user-guide.md` §4. Scripts `C:\tmp\ar1_archives.py` … `ar3_order.py`, measured against the **live** archive API (the catalog CSVs do not exist yet) over a random sample of **60 of 669 public products / 326 archived versions**. Nothing implemented.

  - **The input is the catalog, not the folder — and that is the whole decision.** Archived ZIPs are downloaded on demand (§4.3), so `archives/` typically holds two of a product's forty archived versions. A directory-driven index, which is what §10.5 does for the document doc-classes, would list those two and imply the rest do not exist — publishing a 95%-missing history that looks complete. Entries whose ZIP is absent carry the docsite URL instead of a path, and `available` says which is which.
  - **The payload needs no fallback chain.** `name`, `version_no`, `zipPath` and `GA_date` are present in **326 of 326** children — the opposite of §10.5's PDF titles. `name` ends with the version in 326 of 326, so the item title is `version_no` and the product name goes in the heading once, not forty times.
  - **`GA_date` arrives in two formats**: `November 2022` (54.3%) and `2017-10-23T08:54:14.000Z` (45.7%), all 326 parsing under those two patterns. Normalized to `YYYY-MM` — month is the precision the majority actually has, and synthesizing a day for it would be fabrication.
  - **Order by version, never by date.** The two orders disagree for **9 of the 32 sampled products with 2+ archived versions (28%)**, because maintenance lines ship after their successors: EMS 8.7.0 is dated July 2023, EMS 10.2.1 November 2022. Sorting an archive list by release date interleaves the 5.x and 6.x lines of `loglogic-log-management-intelligence` into a sequence nobody is reading for.
  - **The ZIP filename is not a source of truth, which is §2.8's "use `zipPath` verbatim" seen from the indexing side.** 81% match `/pub/{slug}-{version-dashed}_documentation.zip`; the other 19% preserve a *former* product name (`…composite-information-server-3-1-0…` under the slug `cisco-information-server`, `…apple-ipad…` under `apple-ios`) and the directory varies freely (`/pub/ftl/`, `/pub/sfire-sfds/`, `/pub/ai-suite/3.1.0/`). So neither version nor title can be recovered from the file being linked to. 12 of 12 sampled paths HTTP 200, 0.1–72.5 MB.
  - **7.7% of archived versions are simultaneously live** — 25 of 326, always exactly one per affected product, its current version, which the archive list repeats rather than replaces. Those cross-link to `online-help/` so a reader does not pull 70 MB to read what is published.
  - **`api-references/` deliberately gets none.** 496 of 499 Javadoc-shaped roots in the cache ship their own `index.html` (the 3 exceptions are package directories named `api`, not roots); a second entry point competes with the frame set rather than completing it. Same reasoning that keeps those trees out of conversion. **Flagged to the user rather than assumed** — the request named `archives` only.
  - Probe error worth keeping: the first pass reported **0 of 60 products have archived versions**, because it looked for `children` on `result` when the payload nests it at `result.product.children`. A traversal that "finds nothing" is the failure mode that reads as a finding — 0% is a plausible answer for an archive endpoint, and it would have justified skipping the index entirely.

- [x] **The three document doc-classes get an `index.md` and a `toc.yml` too** (2026-09-09, user decision) — `architecture.md` §6.1/§6.2/§6.2.2, `design.md` §10/§10.5 (+§12 row), Phase 6 checklist. Scripts `C:\tmp\dc1_index.py` … `dc5_pdfmeta.py`, over the same 1,822 versions as the routing survey. Nothing implemented. Until now only `online-help` received navigation, so a reader arriving at `user-guides/10-4-0/` got a directory listing at best; the index is built from §10.4's routed file list because these doc-classes have no source TOC to serialize.

  | Doc-class | Versions with ≥1 file | Files | Median / max | Extensions |
  | :--- | ---: | ---: | :--- | :--- |
  | `user-guides` | 1,228 (67.4%) | 5,088 | 2 / 90 | `.pdf` 100% |
  | `release-information` | 1,612 (88.5%) | 2,988 | 1 / 27 | `.pdf` 1,830, `.txt` 1,154, `.html` 4 |
  | `reference-documents` | 1,590 (87.3%) | 3,557 | 2 / 14 | `.pdf` 2,215, `.txt` 1,265, `.csv` 52, then a 10-extension tail |

  - **Emptiness is expressed by absence.** Doc-classes present per version: 0 → **156**, 1 → 91, 2 → 386, 3 → 1,189. The 156 get no folder and no index; an index that links to nothing is a published dead end, and driving the generator off the routed list makes this fall out rather than need a case.
  - **De-duplicate before copying, not after.** 25 versions carry both `pdf/` and `doc/pdf/` (24 with identical contents) and share **178 filenames**; unhandled, the same PDF is copied and listed twice. One entry per lower-cased filename, root winning over nested — which is what takes `user-guides` from 5,088 files to 5,007.
  - **Titles come from a three-step chain whose obvious first step is the wrong one.** Canonical kind name → PDF Info-dictionary `/Title` → filename stem with separators collapsed. Adding a fifth pattern for `rtu` → Right to Use Terms (235 files) takes canonical coverage to **100% of `release-information` and 97.7% of `reference-documents`**, leaving 83 files for the fallback. `user-guides` never reaches step 1 by construction — it *is* the residue of those patterns.
  - **A byte-level `/Title` regex reads outline bookmarks, and reports success while doing it.** The first probe scanned raw PDF bytes for the first `/Title (…)` and claimed 57% usable — the values were `'Prerequisites for Installation'`, `'Basic Tab'`, `'Table of contents'`, i.e. bookmark labels that precede the Info dictionary in the file. Reading the Info dictionary with a real parser returns the document's own title, like `'TIBCO® Data Virtualization SQL Server Adapter Guide'`. Same family as the `DC.Type` episode above: an unvalidated matcher whose output is plausible enough to ship. ~~78% usable, 22% blank, 0% junk~~ — *corrected 2026-09-09 by the full-population run below: **70.8% / 27.9% / 1.3%**. The 300-file sample overstated usable by 7 points and reported 0% junk where there are 65.*
  - **The cleverness is what produces the garbage.** A fallback that stripped vendor prefix, product tokens and version number yielded 3,352 distinct titles from 5,007 files, **82.9% of them appearing exactly once**, including `'adix 2'`, `'dqid 3'` and `'1 0 0 installation'`. Collapsing `[_.-]+` to spaces and stopping there leaves `tib_ems_users_guide` readable and invents nothing.
  - **The existing templates do not fit.** `config/aem_templates/index.md.j2` and `toc.yml.j2` iterate a nested `guides` tree of Markdown pages; a document index is flat and its items are binaries, so it needs `title`/`path`/`type`/`bytes` and its own pair of templates. Order is kind rank then title, so `release-information` leads with the release notes rather than with whatever the filesystem returned.

- [x] **MadCap Flare converter engine specified** (2026-09-08) — `architecture.md` §5.1 (rewritten from a five-bullet stub into nine measured subsections), §3.4 and §5.4.1 corrected, `design.md` §7.1, Phase 5 checklist. Scripts `C:\tmp\fl*.py`, index at `C:\tmp\flare_inv.json`. Nothing implemented. Surveyed over **676 output roots / 595 versions / 208 products / 422,267 content topics / 307,394 images / 19.5 GB** — Flare is six times DITA and the corpus's dominant engine, and it had had the thinnest spec of the three. Sampled claims rest on 60 whole output roots (59 versions, 48 products, 43,753 files).

  What the survey settled, and the measurement behind each:

  | Decision | Measurement |
  | :--- | :--- |
  | The output root (`Data/HelpSystem.xml`) is the unit of work; the walk **descends** into nested roots | 676 roots, of which **153 nest inside another** (22,456 files — `tp/1.1.0/html/Subsystems/*`). Stop at the first match and 22,456 topics vanish; don't stop and they convert twice |
  | **Each output root converts independently — no cross-root dedup** | 51 of 595 versions ship several roots, sharing 30,736 relative paths. 40.3% byte-identical, ~45% same text, **~15% genuinely different** — `mdm/9.3.2 release-notes/new-features.htm` at a **0.019 token overlap**: two releases' notes at one path |
  | Output **mirrors** the source tree (the opposite of DITA's flat rule, §5.2.2) | Filename stems collide **6.0%** within a root, title slugs 2.6%; 85.3% of topics sit one level down. Mirroring makes link rewriting a suffix substitution |
  | TOC from `Data/Tocs/*.js`; orphans to "Unfiled" | **85.9% coverage corpus-wide** (median 92%), complete in **85 of 676**, below 50% in 59. **`data-mc-toc-path` rejected — 64% of values are an unresolved `[%=System.LinkedHeader%]` token** |
  | The TOC file is read from **`HelpSystem.xml`'s `Toc=`**, not globbed — but every tree in `Data/Tocs/` is used | 673 roots ship one tree, **3 ship two**, and alphabetical-first globbing picks the wrong one in all three. `Toc=` alone is also not enough: in `bctcm/6.2.0` the declared tree covers **53 of 106** topics and the undeclared one covers 50 more. Declared tree is the root, the rest become sibling top-level nodes |
  | The tree carries **no titles** — reconstruction is a two-pass index inversion | `<Name>.js` `tree` is `{i,c,n}` integer ids only; `<Name>_Chunk<N>.js` is `path → {i:[ids], t:[label], b:[anchor]}`. **39,488 tree nodes in the sample, 0 unresolved.** `i`/`t`/`b` are **parallel arrays** (0 ragged in 37,598 entries) — **3.7%** of page entries sit at several TOC positions and 60 carry a different label at each; `b` is a bookmark on **12.1%**. All **679** TOC files corpus-wide are `numchunks:1`, so the sharding path is implemented but untested |
  | **The landing page is converted and hoisted to the first `toc.yml` node** | `HelpSystem.xml`'s `DefaultUrl` resolves in **676 of 676** roots but is **not a TOC entry in 55 of 60** sampled — it would otherwise fall under "Unfiled". Not boilerplate either: 0 hits for lorem/`[Enter …]`/`TBD`/`[%=…%]` across all 676, and past the hero furniture **351 rich / 270 light / 55 hero-only** |
  | **A section page is generated for every headless TOC node** | 7,388 container nodes; 7,223 (97.8%) have a real page, **165 (2.2%) are headless** — the `'___'` sentinel, label and children and no file. **151 of the 165 are top level**, holding 1,357 children. AEM treats a childed node with no page as a broken parent |
  | Two title strings, both kept | `<title>` disagrees with `h1` in ~10% and is always the truncated one; the TOC label equals `h1` in **2,429 of 2,512**, the rest being deliberate short nav labels |
  | One content selector, no fallback chain | `div[role='main']#mc-main-content` in **4,656 of 4,660 MadCap topics (99.9%)** |
  | **Strip `#feedback-survey` before counting any link** | It is in 95% of topics and is **47.7% of every raw `href`** (`javascript:void(0)`). After it: 1.1 links/topic, 78% relative `.htm`, **98.3% resolve on disk** |
  | Semantics come from `data-mc-autonum`, not from classes | Callouts Note 1,574 / Warning 30 / Tip 28 / Important 21; task structure exists **only** as autonum values (`Procedure` 702, `Subtopics` 467, `Before you begin` 244). Exact mirror of DITA, where the label is a `span` to be *deleted* rather than an attribute to be *recovered* |
  | **MadCap emits lists as tables** | 1,321 `AutoNumber_p_*` single-column tables in 7% of topics (`Bullet` 753, `Step` 480), against 1,809 `<ul>` / 1,201 `<ol>`. The autonum attribute decides ordered vs bulleted; the class name does not — `Step` and `Bullet` appear both ways |
  | 57% of tables GFM-safe, fences bare | 98% of tables are `TableStyle-Table` (pure styling); 1,565 of ~1,600 `<pre>` carry no language |

  - **Two of §3.4's seven Flare markers were misfiring, and dropping them costs nothing.** Over all 1,822 cached versions the list matched **689 against 595 holding a runtime** — `Skins/` 91.4% precise, `Data/` 88.0%, both generic directory names other publishers use. **All 94 false positives come from those two markers and no other** (43 both, 38 `Data/`, 13 `Skins/`): 17 WebWorks, 3 DITA, and 74 carrying no engine marker at all, mostly 2010–2013 adapter packages. Each would have been handed to the Flare converter — the silent-wrong-output failure §3.4 exists to prevent. **`*.mcwebhelp` alone finds all 595; `csh.js` alone finds all 595.** `design.md` §7.1 carries a regression fixture: a WebWorks tree shipping a `Skins/` directory.
  - **A first attempt at those precision figures produced a different set, and reconciling the two is the finding.** Re-deriving them found 631 matches and 36 false positives with `Skins/` at 100% — against the 689/94/91.4% already written down. Neither was wrong: the first probe matched directory names **case-insensitively**, the second exactly. The gap is 56 versions with a lowercase `skins` and 45 with a lowercase `data`. It matters because a naive Windows implementation is case-insensitive by default, so 689/94 is the number a real detector meets — and because `Data/` misfires under *both* rules (36 versions ship a correctly-cased `Data` with no Flare runtime), which is what settles dropping it rather than specifying a case rule. **This is the exact inverse of the DITA `DC.Type`/`DC.type` episode logged above**: there a case-sensitive matcher invented an absence, here a case-insensitive one inflated a presence. Case sensitivity is a per-signal decision in this corpus and is now stated as one in `design.md` §7.1.
  - **The old §5.1's headline feature described a construct the corpus does not use.** "Dropdown Extraction — unrolls `MCDropDown`" was bullet one. A cache sweep found 459 matching HTML files of which **457 are the generated `_templates/Home.htm` landing page**, leaving **exactly one content topic** in 422,267 (`tp/1.1.0/html/UserGuide/previous-versions-release-notes.htm`); sampling agrees at 0 in 4,810. The classes saturate every root's skin CSS and `MadCapAll.js` — separate skin from content and the feature disappears. This is the same failure mode the ledger keeps recording, inverted: a *positive* result from a matcher that never distinguished generated files from content.
  - **The predecessor's Flare pipeline is a genuine reference — unlike its DITA sketch — and still has three measured gaps.** It has actually run at scale, and its 13-pass ordering, `fake_list_tables()`, `split_colspan_tables()` and `SPAN_TO_TAG` vocabulary are worth inheriting; `fake_list_tables()` independently found the `data-mc-autonum` ground-truth rule. But: **`div.topic-frame` is missing from its chrome list** though it wraps content in 88% of topics; its four-selector content fallback **converts non-Flare files rather than reporting them**, which is how a Javadoc page reaches the Markdown output; and `skip_path_segments` is a hand-accumulated mix of product and language names (`/javadoc/`, `/Java_API/`, `/golang/`, `/c/`, `/tibdg/`) — the exact failure mode §6.3's marker-based `is_api_reference()` was specified to end.
  - **§5.4's "863 `Alias.xml`" was over-attributed to Flare; the entry counts are unaffected.** By where each file sits: 674 in a detected Flare root (10,239 entries), 153 nested inside one (772), 5 in a partial Flare tree (43), **30 in WebWorks trees (all zero-byte, 0 entries)**, 1 elsewhere (0 bytes). 10,239 + 772 + 43 = 11,054, so every design decision built on the entry counts stands. Corpus-wide zero-byte count is **31**, not the 11 the 2026-09-04 subset showed.
  - **Two probe errors, both recorded because the first answer was wrong and looked right.** (1) An early duplication figure read "13% byte-identical", which made cross-root dedup look obviously safe; it was computed over generated directories and only the first two roots per version. Corrected: **40.3% byte-identical, ~85% same content, and it is the remaining 15% that rules dedup out.** (2) A `du -sh`, an 8-pass `rg` loop, a 600s `difflib` comparison and a whole-cache Python scan all had to be abandoned for being scoped to the entire 2.2M-file cache rather than to the population in question — the survey cost far more wall-clock than it needed to, and the fix each time was to scope to the 676 roots first.
  - **Three earlier conclusions in this same entry were wrong, and the corrections came from reading the TOC tree instead of the flat path map** (2026-09-09, prompted by the requirement that the landing page lead `toc.yml` and that childless section nodes get a page):
    - **`i`/`t`/`b` are parallel arrays.** The first pass read `t[0]` for every id in an entry, which relabels every node sharing a key — visible only on the `'___'` sentinel, where one entry carries three unrelated book titles. 0 ragged arrays in 37,598 entries confirms the alignment. The published "3.8% multi-position" figure counted sentinels; the real figure is 3.7% of *page* entries, and 60 of them change label between positions.
    - **"`_templates/` is generated, none of it converts" was false.** It holds the `DefaultUrl` landing page (676 of 676 roots resolve) and ~~162 TOC-referenced entries across 59 of 60 roots~~ — **1,737 across 660 of 676**, re-measured 2026-09-09. The "5,136 generated HTML files" figure is now 2,339.
    - **"Dropdown unrolling is removed as a feature" was half right.** 457 of 459 matches being `_templates/Home.htm` was read as evidence the construct is dead. It is dead *in content topics* (0 of 4,810) and alive on the landing page, which is now converted — so the feature moves rather than disappears. **The same evidence supports the opposite conclusion once the scope of conversion changes**; the finding was fine, the inference from it was scoped to an assumption that did not hold.
  - **The predecessor skips `Home.htm` by filename**, which is why its output has no landing page in 644 of 676 roots. Its `skip_filenames` list is otherwise worth inheriting; this one entry is now an explicit reject.
  - **What is *not* a hazard, stated so it does not get designed around later:** encoding. 0 of 4,810 sampled topics fail a strict UTF-8 decode. Console output rendering as `TIBCO GridServer?` is a Windows codepage artifact of the probe, not a property of the files.

- [x] **SDL DITA converter engine specified** (2026-09-08) — `architecture.md` §5.2 (new; "Universal Asset Preservation" moved to §5.5 so §5.4's widely-cited CSH subsection numbers survive), Phase 5 checklist. Scripts `C:\tmp\an_dita*.py`, index at `C:\tmp\dita_docsets.json`. Nothing implemented. This closes the `planning.md` placeholder that said the DITA handler was "a placeholder line, not a design". Surveyed over **353 doc-sets / 319 versions / 136 products / 67,406 `GUID-*.html` topics**; where a claim is sampled, the sample is a whole doc-set, so per-doc-set properties are measurable at all.

  What the survey settled, and the measurement behind each:

  | Decision | Measurement |
  | :--- | :--- |
  | Content is `<article>`, then chrome is stripped from *inside* it | `<article>` in 3,742/3,742 topics (100%) — the only invariant across two coexisting skins (Bootstrap 89%, legacy `#leftbar` 11%). Inside it: `#copyright` 98%, `<noscript>` 89%, `#thumbnailDialog` 89%, `.familylinks` 85% |
  | Titles from the topic, structure from `suitehelp_topic_list.html`, orphans to an "Unfiled" node | `h1` == `DC.Title` == `<title>` in 100%. `suitehelp_topic_list.html` covers median 98% but is complete in **2 of 314**; `toc_crawler.html` exists in only 54 of 353; their union is complete in **36 of 353**. They disagree on 86 of 2,409 shared titles (4%) and `toc_crawler.html` is the stale one in all five spot-checks |
  | **`DC.Relation` rejected as a parent pointer** | 97% of topics carry one and every target exists — but **42% sit in a mutual `a↔b` pair and all 65 doc-sets tested contain a cycle**. It is DITA's related-links relation; no tree can be built from it |
  | Flat output, deterministic slugs, `_unique_N` collapsed | 44% of doc-sets contain a title collision (106 of 3,675 topics). 2.7% of topics are the same topic republished at a second TOC position — identical but for `_unique_N` on every id and anchor (byte-diffed on `marketo/7.1.0`) |
  | Callouts → GFM alerts with the label span removed; fences bare | 1,764 callouts, **each with a matching `span.*title`** (1,674 `span.notetitle` for 1,674 `note` divs). 864 `<pre>` blocks yield exactly one language attribute in the whole corpus |
  | Rewrite links by GUID, not by suffix; drop redundant fragments | **17,043 of 17,046 GUID links resolve (0.02% broken)** and all 2,650 content images exist — link integrity is the *opposite* of Flare's. But 48% of hrefs are `GUID-….html`, 15 are extensionless, and **84% of fragments are redundant self-references**; 6.1% of the real ones dangle |
  | Images keep their source filename | **1,656 of 1,819 `<img>` (91%) carry no `alt` at all**; only 4.6% have usable alt |
  | `GUID-*-homepage.html` feeds `meta.yml`, never converted | Present in 314 of 353 doc-sets and **all 314 carry `publication-title`, `release-version`, `release-date`**; its `<article>` holds those three divs and no body |

  - **The predecessor's DITA implementation is a sketch, not a reference**, and reading it rather than trusting it paid three times: its callout transform strips `span.note__title` (the *other* DITA flavour) and never `span.notetitle`, so every SuiteHelp callout would emit a duplicated label; **there is no `<a href>` rewriting anywhere** in its four-step pipeline, so 48% of links in its output would point at files that do not exist; and it has essentially never run — one rename map in the whole cache, and an output directory holding a `toc.yml` reading `docs: []` and zero `.md` files.
  - **The 371 turned out to be two publishers, and chasing that down cost one wrong answer first.** §3.4's rule is `GUID-*.html` **or** `DC.*` meta; the GUID population is 316 (319 corpus-wide). The ~55 remainder is the predecessor's `file_dita` — file-named topics under `topics/`, **132 doc-sets / 66 versions / ~6,215 topics, entirely Spotfire** (`sf-pysrv` 27, `sf-rsrv` 21, `enterprise-runtime-for-R` 11, five more). Its transform half is the same DITA (`topictitle1` 100%, `shortdesc` 99%, `note` 58%; `DC.identifier` still a GUID in 98%; `DC.relation` *plural* in 86%, which strengthens the refutation above). Its **layout** is not: a nested tree with a `_shared/` directory, against SuiteHelp's flat doc-set. So §5.2 is scoped to SuiteHelp, `engines/dita.py` branches on whether a doc-set holds `GUID-*.html`, and the file-named flavour is skipped with a report line until it gets its own layout survey. *(Superseded 2026-09-09 on two points: "entirely Spotfire" is wrong — `ebx-addon` publishes it too, 9 products in all — and there will be no layout survey, because all 9 are out of scope. The branch stays as a guard; see the scope decision in §3.)*
  - **A case-sensitive regex produced a confident wrong correction, and it is recorded because it nearly landed.** Probing for `name="DC.Type"` returned 0 file-named versions, which looked like clean evidence that `file_dita` did not exist in this corpus — and, combined with a recount, like evidence that the published 371 was inflated to 316 by the rejected `screen.css`+`print.css` signal. Both conclusions were written into `architecture.md` §3.4 before the next probe showed the flavour writes **`DC.type`** in lowercase. **371 was correct all along** (316 + ~55), the coverage claim stays 68% → 98%, and `design.md` §7.2 now states the case-insensitivity as a rule with a test attached. The failure mode is the one this ledger keeps hitting: a null result from an unvalidated matcher reads exactly like a real absence.
  - A first pass at the `DC.Relation` test assigned depth only after recursing, so cycles produced plausible-looking but wrong numbers. It is recorded because the *corrected* result is what rules the approach out — the same failure mode as the 2026-09-07 WebWorks descoping, where an unmeasured premise drove a decision that had to be reversed.

- [x] **WebWorks converter engine specified** (2026-09-09) — `architecture.md` §5.3 (new; CSH moved §5.3 → §5.4 and "Universal Asset Preservation" §5.4 → §5.5, with every cross-reference in five files updated in the same commit), `design.md` §7.1/§7.2/§12, Phase 5 checklist. Scripts `C:\tmp\ww*.py`, index at `C:\tmp\ww_census.json`. Nothing implemented. This closes the last of the three `planning.md` engine placeholders; only DocBook remains unspecified. Surveyed over **691 books / 195 versions / 110 products / 38,818 HTML topics / 32,690 images / 1.49 GB**. The two whole-DOM probes read every element of 29,712 topics — the seven books over 400 topics are the embedded API-reference trees and are excluded by the §6.3 predicate, not by size.

  This engine is the corpus's oldest (2004–2015) and its most and least tractable at once. **Nothing it emits is semantic HTML** — 121 `<ul>`, 25 `<ol>`, 455 `<li>`, 60 `<pre>`, 107 `<th>` and 334 headings of any level across 29,712 topics, against 1.81M `<div>` and 955K `<td>`. Every list is a table, every heading is a classed div, every code block is a run of sibling divs. Against that, its runtime metadata is the best of the three engines: TOC, file index, book titles and CSH map are all present, all consistent and all machine-readable.

  What the survey settled, and the measurement behind each:

  | Decision | Measurement |
  | :--- | :--- |
  | **`wwhdata/` alone is an exact detector** | Over all 1,822 cached versions: **195 matched, 0 false positives, 0 false negatives**. `wwhelp/` 91.3% recall; the predecessor's `wwhelp/books.htm` **72.3%**; the generator meta tag **1.5%**. Lowercase everywhere, no case variants |
  | The **book** is the unit of work, the **collection** is the doc-set | A book holds `wwhdata/`; a collection's `wwhelp/books.xml` names a `<Book directory="X"/>` with X ≠ `.`. 157 collections declare 602 of 691 books. Books per version: median 3, mean 3.5, max 30 — the reverse of Flare |
  | **`l=` indexes `files.js`, not `files.htm`** | Resolving all 39,647 anchored TOC entries through both orderings and checking the anchor is present: **`files.js` 99.28%, `files.htm` 88.87%**, 86 books strictly worse, several at 0 |
  | Percent-decode `directory=` and every `files.js` href | 22 declarations resolve only after decoding (580 → **602 of 602**); corpus TOC coverage 85.7% → **87.1%**, sub-50% books 19 → 12 |
  | Preserve `books.xml` order; take names from `title.js` | Declared order is **not alphabetical in 112 of 157 collections (71%)**. `<Book>` carries only `directory` and `encoding` — **no name attribute anywhere** |
  | Content is `body > blockquote` | **29,594 of 29,712 (99.6%)**, never nested, **never more than one per file**; per book 634 at 100% and 5 at 0%. **All chrome is outside it** — the reverse of Flare |
  | Lists are tables, and the shape is exact | `div.X_outer > table > tr > td[div.X_inner] × 2`, and the `_inner`:`_outer` ratio is **exactly 2.000** for Bullet (232,846/116,423), Step, ListDash and StepInd |
  | Table layout-vs-content is decided by the **parent class**, not `role` | `role="presentation"` marks 149,326 layout tables and **misses 86,702 identical wrappers** from output predating the attribute. Content tables: no `<th>` (45 corpus-wide), `CellHeading` in row 1 in 98.5%, **50.5% GFM-safe** |
  | Cross-references are JavaScript, and recoverable | **89,125 `WWHClickedPopup`, every one anchored** — the only JS function in the corpus. Same-book **100%** resolved, cross-book 52.6%, and the Flare version-wide fallback rescues **0** of the 711 dangling |
  | Emit an anchor only where referenced | **1,319,510 `<a name>`** in content (~43/topic, 96.5% bare integers); **85,219 (6.46%)** are actually targeted, and references resolve at **99.81%** |
  | Figure captions precede their figure | **7,316 of 7,442 `div.FigureTitle`** are followed by the sibling holding the image; 97.4% of `div.TableTitle` are last in a `<caption>` |
  | Images keep their filename; `alt` is a skin marker | The **22,126 `<img>` with no `alt` are precisely the 22,126 under `images/`**; all 14,467 `tpl/` icons have one. Same finding as DITA's 91%, from the opposite direction |
  | Decode by declared charset, default UTF-8 | Of 37,658 files, **213 fail a strict UTF-8 decode and all 213 declare `iso-8859-1`** |

  - **The predecessor's WebWorks pipeline is a real implementation with six measured defects**, and it gets the hardest thing right: `soup.find("blockquote")` is the correct container. What it gets wrong is systematic rather than incidental — it resolves the TOC through `files.htm` (wrong topic, not missing topic, in 86 books); `_inline()` returns only the text of an `<a>` without `href`, discarding **1.29M anchors** that 57.5% of TOC entries, 100% of popups and 43% of CSH entries target; `_SPAN_MAP` maps `LiveLink` to plain text, **deleting 85,719 cross-references**; the admonition handler reads the icon cell rather than the sibling prose cell, so all 12,866 callouts emit empty; `CodeLine` fences per div, so a five-line command becomes five fences; and it detects on `books.htm` at 72.3%. Worth taking: the container selector, `_SKIP_FILENAMES`, the `N?Heading` numeral→level mapping, and `_SPAN_MAP` minus `LiveLink`.
  - **The `wwhelp/` doc-set error, made a second time.** A first pass took the parent of any `wwhelp/` to be the doc-set and reported "643 doc-sets with exactly one book, 180 with none" — both artifacts of the wrong denominator, because **every book ships its own self-contained `wwhelp/`** declaring `<Book directory="."/>`. This is the same shape as the 2026-09-07 CSH mistake in the same package: `wwhdata/` is per book, `ctx/` is per doc-set. The discriminator has to be the *content* of `books.xml`, never the presence of a directory.
  - **Two XML twins from one generator, one faithful and one not.** `wwhdata/xml/toc.xml` matches `toc.js` in **0 differences across all 641 books** shipping both, while `wwhdata/xml/files.xml` is the lossy twin of `topics.js` that §5.4.4 rules out. Neither can be inferred from the other, and the JS is the source of record in both cases.
  - **Three numbers were wrong before they were right, in the three ways this ledger keeps recording.** (a) A book at *5.6% TOC coverage* was a percent-encoding bug in the probe, not a broken book — the fix moved corpus coverage 85.7% → 87.1% and is now a rule in the spec. (b) A *"623 of 646 books have a title mismatch"* came from comparing each book's `title.js` against the **first indexed file's** `<title>`, which is the title page; comparing each topic to its own gives **98.4% equal**, all 465 differences `&nbsp;` padding. (c) The blockquote invariant read **87.5%** until the denominator was scoped to the population the engine actually converts — 4,552 of the 4,763 missing topics are embedded API references. Same correction Flare's TOC coverage needed, and the general form is already in this ledger: *a ratio between two counts is only evidence if the counts are of the same kind of thing.*
  - **§6.3's marker list needs one addition.** Three `activespaces_remote` books embed a **Sandcastle** .NET reference (3,344 files) whose root is `Index.aspx` / `FillNode.aspx` carrying `Sandcastle Help File Builder`. The listed `fti/FTI_*.json` marker does find the tree, but one level down, so it claims a narrower root — and until the predicate claims it these books read as 0.1% TOC-covered and 0% blockquote, which is three of the twelve sub-50% books.
  - **Two genuine sub-populations are converted rather than dropped**, both small enough to name: the **45 stripped books** (`wwhdata/files.htm` and nothing else — 1,395 topics, 98.9% indexed, no navigation recoverable) and the **5 books / 118 topics of 2004–2006 output** that use real `<h2>`/`<p>`/`<pre>` and have no blockquote at all. The second is the entire population of the `p*` class family in the vocabulary histogram, and it is detected by the container test rather than by a date or a product name.

- [x] **Doc-class routing settled: source folder first, then name** (2026-09-07, user decision) — `architecture.md` §6.2/§6.2.1, `design.md` §10.4, Phase 6 checklist. Surveyed against the extracted cache: **1,822 versions, 11,633 documents** in `pdf/` and `doc/` (scripts `C:	mpn_docclass*.py`).

  The rule the user gave, and what the corpus says about it:

  | Source | Rule | Doc-class | Measured |
  | :--- | :--- | :--- | ---: |
  | `pdf/` | release note | `release-information` | 1,829 |
  | `pdf/` | VPAT, licence, reminder notice | `reference-documents` | 2,214 |
  | `pdf/` | *default* | `user-guides` | 5,088 |
  | `doc/` | readme | `release-information` | 1,159 |
  | `doc/` | *default* | `reference-documents` | 1,343 |

  - **The folder discriminates before the name, and before the extension.** `pdf/` is flat and holds the authored deliverables (6,795 PDFs, no subdirectories); `doc/` holds boilerplate (2,419 TXT — reminder notices, licence details, RTU statements). Extension-based routing would have to explain why licence PDF and licence TXT agree while user-guide PDF and readme TXT do not.
  - **`doc/` needs exactly one name test.** Licence, reminder notice, RTU and the CSV/XLSX/HTML tail all share one destination, so the only question is *is this the readme*. The licence and VPAT patterns exist solely for `pdf/`, whose default is `user-guides`.
  - **Both folders occur at two depths** — 1,123 versions at the package root, **373 nested as `doc/pdf/` and `doc/doc/`**. Handling only the root layout would publish nothing for a fifth of the corpus.
  - **`user-guides` is the default, not a match**, so there is no unclassified bucket and an unfamiliar guide name is published rather than dropped. Residue after routing: **4 files**, all genuine guides containing the word "note" (`special-notes`, `liveviewweb_newnote`).

  **The separator is where this breaks.** A first pass anchored on `` misrouted **1,753 `*_relnotes.pdf` into `user-guides`** — `_` is a word character, so `` never matches between `_` and `rel`. The corpus also spells these names with spaces (`mft platform server v7.1 for windows release notes.pdf`) and both ways round (`licencing` beside `licensing`). This is the predecessor's substring bug seen from the other side: there the boundary was too loose and swallowed 8.5% of the corpus, here too tight and dropped 1,753 files. Patterns are now written against observed spellings and tested as such.

- [x] **Flare alias corpus re-measured over the full cache** (2026-09-07) — **863 `Alias.xml` files, 387 with content, 11,054 entries, 2,396 distinct names**, superseding the 2026-09-04 subset (272 / 196 / 7,220). Script: `C:	mpn_flare_csh.py`. One published number was materially wrong and is corrected everywhere it appeared (`architecture.md` §3.9/§5.4.1, `design.md` §6.2/§6.3, `planning.md`, `user-guide.md` §2, `catalog.py`, `models.py`): **empty and zero-byte alias files are 55% of the corpus (476 of 863), not 28%**. 445 are well-formed `<CatapultAliasFile />`, 31 are zero-byte. This strengthens rather than complicates the two-column design — `_has_csh=true` with `_csh_names=0` is the *majority* case, not an edge case, which is exactly why the boolean and the count are not collapsible into one column.
