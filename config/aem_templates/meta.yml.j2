{#-
  PLACEHOLDER (2026-09-09). The AEM side has not supplied a meta.yml spec, so the
  field list below is a Phase-1 scaffolding guess measured against nothing. It is
  not a contract: docs/design.md section 10 says so, and no verification rule
  asserts its content. One input is already grounded and will want a home here --
  SuiteHelp's GUID-*-homepage.html carries publication-title, release-version and
  release-date in all 314 doc-sets that ship one (architecture.md 5.2.7).
  Rewrite this file against the real details when they arrive.
-#}
# AEM metadata descriptor for {{ product.display_name }} {{ version.version }}
# Generated by DocuShift (Stage 6) -- do not edit by hand.
version: "1.0"
product_code: "{{ product.product_code }}"
display_name: "{{ product.display_name }}"
business_unit: "{{ product.bu }}"
family: "{{ product.family }}"
product_version: "{{ version.version }}"
release_date: "{{ version.release_date or '' }}"
is_archived: {{ 'true' if version.is_archived else 'false' }}
source_engine: "{{ version.engine }}"
source_zip: "{{ version.zip_url or '' }}"
converted_at: "{{ converted_at }}"
docushift_version: "{{ docushift_version }}"
