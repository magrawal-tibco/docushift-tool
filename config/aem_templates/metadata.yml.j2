{#-
  The AEM metadata descriptor (contract supplied 2026-09-10). Two keys exist:
  `csg-product` at product level and `csg-version` at version level, the version
  dotted as the product names it -- the folder carries the dashed form. The ~11
  fields the Phase-1 `meta.yml.j2` invented were measured against nothing and are
  deleted rather than kept alongside; a guess shipped beside a contract reads as
  part of it. `level` is prose for the comment, `values` is the contract.
-#}
# AEM metadata{% if level %} ({{ level }} level){% endif %}
# Generated by DocuShift (Stage 6) -- do not edit by hand.
{% for key, value in values -%}
{{ key }}: {{ value | yaml }}
{% endfor -%}
