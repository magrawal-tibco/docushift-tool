{#-
  PLACEHOLDER (2026-09-09). The AEM side has not supplied a nav.yml spec, so the
  shape below is a Phase-1 scaffolding guess measured against nothing. It is not
  a contract: docs/design.md section 10 says so, and no verification rule asserts
  its content. Rewrite this file against the real details when they arrive rather
  than growing it out of the guess. toc.yml is the specified navigation artifact.
-#}
# AEM top-level navigation for {{ product.display_name }} {{ version.version }}
# Generated by DocuShift (Stage 6) -- do not edit by hand.
version: "1.0"
nav:
  - title: "{{ product.display_name }}"
    path: "/{{ product.bu }}/{{ product.family }}/{{ product.product_code }}/{{ version.version }}/"
    children:
{%- for guide in guides %}
      - title: "{{ guide.title }}"
        path: "{{ guide.path }}"
{%- endfor %}
