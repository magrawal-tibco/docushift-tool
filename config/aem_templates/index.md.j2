{#-
  A generated page's body: the version index, and every navigation node that has
  children and no page of its own (design.md section 10). **Body only** -- the
  frontmatter is the converter's, written by the same `_render` that writes a
  converted topic's, so `title:` and `generated: true` exist in one place and
  cannot disagree with the page they head.
-#}
# {{ title }}

{% for child in children -%}
- [{{ child.label }}]({{ child.path }})
{% endfor -%}
