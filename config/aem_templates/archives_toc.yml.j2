{#-
  The archived-versions table of contents (Stage 6d, design.md section 10.6).

  **One item, pointing at `index.md`** -- the same rule as
  `documents_toc.yml.j2`, and Phase 9 applied it here on purpose rather than by
  sweep. These entries had the one real argument for staying: they are catalog
  rows, not files in the folder, and most have no local ZIP at all, so unlike a
  PDF they were never implying a page-per-entry. They collapse anyway, because
  one rule for every generated `toc.yml` is worth more than a defensible
  exception.

  The version history is not lost -- `archives_index.md.j2` keeps all of it,
  including `available` and the per-version link, which is what
  `architecture.md` section 6.2.3 actually argues for. This file just stopped
  being a second copy of it.
-#}
# AEM table of contents{% if title %} for {{ title }}{% endif %}
# Generated by DocuShift (Stage 6) -- do not edit by hand.
items:
- title: {{ title | yaml }}
  path: "index.md"
